# Meta 协作决策记录

## D-092 — 直接读取验收结论也必须核对当前持久Spec

- 新增测试证实：只在verify()中检查完整契约哈希还不够，candidateHasPassedMetaReview直接读历史收据时会在readScope变化后仍返回true。低风险完成和Owner入口依赖这个判断，不能等复测发生后才阻止旧结论。
- 将已有Spec字段提为统一构造器，v3的两条收据保存specIdentityHash，所有直接配对读取都与当前持久Spec重算值核对。旧收据保持原样并拒绝作为v3；合成政策测试夹具显式升级，保留实际生产协调器测试，未放宽断言或权限。
- 此处不宣称外部运行配置在所有独立入口均实时生效；命令定义/源码清单/模型策略当前在verify协调器中参与完整契约，其他入口仍需下一轮审计与真实接线验证。

## D-091 — 被测实现采用显式可信清单和当前验证节点范围，不凭 import 扩权

- 原验收模型只收到测试文件，无法核对被测实现。新增命令级 acceptanceSourceFiles，按仓库相对路径读取同一固定候选Git对象；仅受信任配置能提供清单，继续套用当前验证节点的允许/禁止范围。复用既有路径匹配语义，禁止祖先目录和敏感目录另作拒绝，先校验全体选择再读取。
- 实现上下文与测试来源有明确角色，模型绑定实现文件会被确定性拒绝；两个模型都可看辅助逻辑，但不能把普通实现、注释或函数名当通过证据。测试/实现总数和单项/总输入仍有界。
- 验收契约哈希包含源码清单及当前read/deny范围，避免配置变更复用旧通过结果。无配置时不自动搜集依赖，未知依赖保持缺失；此阶段不承诺自动依赖图、非JS/TS资源或真实业务覆盖。真实模型与完整回归仍须单独验证。

## D-090 — 源码与自然文本分别脱敏，语法完整不等于业务证据完整

- 普通文本正则会把 password === 中的比较符当凭据值隐藏，且留下后面的实际字面量。改用已安装 TypeScript 的有界 parse-only AST 视图，两个源码边界统一处理；同版本依赖从开发项移至运行项，不能依赖本地 node_modules 掩盖打包缺失。
- 隐藏凭据字面量及常见敏感文本/注释，保留无敏感值的引用与比较、原有换行及非敏感行。仅拼接静态字符串键，动态键按未知敏感处理，不执行表达式；未知或损坏语法静态失败关闭。解构别名、转义键、多行/标签模板和重放都补回归。
- AST 处理器不是污点分析或通用 Secret 扫描器，脱敏输出不可执行且不承诺语义等价。真实 Astra 独立复核继续拒绝局部函数替代真实登录/界面覆盖；不恢复隐藏值，不按测试名放行。被测依赖的显式读取边界和真实 Docker 通道仍是独立待办。
- TypeScript引入后真实headless烟测发现CJS位置全局未定义，而普通index烟测仍绿；因此提供bundled ESM兼容映射，并将headless无node_modules启动/健康/SIGTERM检查纳入默认回归。保留现有独立打包检查，接受当前约11.5MiB headless体积，不把通过构建当作运行通过。

## D-089 — 凭据样式示例不进入验收说明，业务覆盖不足不能降格为通过

- 保留敏感输出门禁，增加两个角色共享的说明规则及拒绝后收据不保留示例值的测试。凭据标识仍由系统给出，不改变来源/候选/条件校验。
- 真实独立复核正确拒绝“局部状态函数等于文案”代表完整保存流程的合成提议。另设精确函数契约仅测试映射通路，明确不作为真实业务验收；弱断言使用合成提议和真实独立复核，避免把上游拒绝或传输失败当复核通过。
- 发现未解决限制：自然文本脱敏器将源码 password === 的运算符隐藏；源码采集器当前仅收集显式测试文件，不含被测依赖源码。不能借本批提示修复宣布这两项已解决，也不应通过忽略缺失上下文让验收绿灯。

## D-088 — 验收身份由主程序提供，模型仅做语义选择

- 真实模型无法可靠计算未提供的 SHA256；先前合成端口自行预计算掩盖接线缺口。现给两个角色同源条件标识，保留完整的独立校验和不变的收据格式。
- 模型说明中复述看似凭据的示例仍由原敏感输出门禁拦截；这需要另行对齐输出契约，不能以放宽Secret保护换取测试通过。

## D-087 — 容器模型通道不得靠伪造来源绕过无密钥边界

- 真实 Docker 请求收到 origin_rejected，已与 OpenCodex 本机 Host 安全策略对应，不归因为需要用户另找 API 密钥。
- 保持现有 OpenCodex 配置及产品 loopback-only 限制；新增容器到当前模型的受限本机通道之前，先说明授予调用能力的影响并请求唯一 Owner 批准。不改变身份/凭据、不对公网开放、不自动放宽全局安全策略。

## D-086 — 将可回答问题边界同时用于生成 schema 与运行时验证

- 真实持久化调用错误回答 natural-input-pending；笼统的“natural- 问题”提示与系统保留编号存在歧义。只修改提示不足以对齐输出契约，因此按当前业务问题生成枚举，无可回答问题时 maxItems=0，保留独立运行时拒绝。
- 不移除上下文完整性状态、不让模型清除系统标记、不静默过滤错误回答后当成功。原文确认修复一起验证；当前消息和来源仍属不可信材料。
- 合成真实探测中，同群第二条模糊消息可能进入归属澄清而非立即建事项，不能直接断言 intake job 存在。独立的模糊需求/重启用另一合成群，明确这只验证 intake，不替代自然归并或真实群能力验收。

## D-085 — 对齐自然目标生成与原文确认契约，不移除来源门禁

- 真实 Astra/medium 输出将用户的“把”润色为“将”、添加“在”，quote 正确但 goal.text 非原文，生产校验以 natural_intake_confirmation_not_grounded 拒绝。原提示只要求 quote 逐字，没有说明已确认目标文本也必须逐字，生成与校验契约不一致。
- 在模型指令中明确连续原文、当前目标上下文确认的唯一例外、长文本或必要归纳保持未确认。用户仍自然表达，明确目标不为润色多问一遍。未采取直接信任 quote、自动将任意模型目标标已确认、或用引用替换任意错误目标的做法。
- 保留现有拒绝逻辑，新增模型契约、自然需求入账/重放与四类错误确认测试。真实语义复测与完整回归证据见 VERIFY；模型接口成功不等于真实钉钉群交付通过。

## D-084 — 直接接 OpenCodex 的显式回环流式通道，不修改 OpenCode

- 用户已纠正产品名为 OpenCodex；不再沿 OpenCode launcher 认证错误扩展修改权限。现有本机 OpenCodex 的 Astra/medium 无密钥真实请求已返回 OK，其协议要求消息数组和 stream=true。
- 通过既有模型端口新增 opt-in opencodex_local：无认证只允许指定字面回环地址，保留默认凭据模式；每个角色独立选择，不借用其他角色/环境凭据。推理强度显式发送且由完成事件核对，不信任目录声明或模型自述。
- 流中间文本不直接用于需求或验收。逐项核对 message/text done，等待 completed 后仍检查流终止；completed.output 缺正文是已观察的 OpenCodex 行为，接受有一致完整事件证据的文本，不接受冲突正文或错误后续事件。未知事件失败关闭。
- 取消/超时同时约束 fetch 和 body；传输忽略取消时也清理迟到 body，类型错误的 body 不遗留连接。管理令牌不进入本通道。Docker、真实语义效果及六类试点必须另外验证，不能根据最小结构化请求宣布产品完成。

## D-083 — 丢失创建回执时先精确发现并持久绑定，再按 ID 恢复

- 原恢复只选择已知 ID，创建回执永久丢失时无法恢复。现在允许从旧实例不可变账本中的精确随机名称发现；不依据名称前缀或缺失回执直接清理。
- inspect 同时验证唯一结果、完整 ID、名称、标签和原固定镜像。ID 入账必须绑定当前实例租约、原资源快照和最新恢复次数；写入成功先于任何删除，最终独立查缺才算恢复成功。
- 未找到名称仍保留未知，因为迟到创建可能尚未出现。原三次尝试预算跨重建保留，耗尽不自动重置；取消、接管、冲突迟到回执和写入失败均拒绝删除。
- 本批证据为生产提取器/恢复器、真实 SQLite 与受控 Docker 命令端口；没有删除实际容器，不证明真实强杀、独立 supervisor、主机重启、模型或钉钉六场景完成。

## D-082 — 发送与核查都必须在实际执行/落账时检查租约

- 反例证明：维护等待后复用轮询起始时间会让过期实例继续发送或查询；普通发送complete同样使用旧时间，会将超期回执记为sent。改为各异步边界读取当前时间，发送complete使用已有包含传输耗时的deliveredAt检查实例和claim有效期，不自动续租弥补过期。
- 维护后检查失败沿既有lease_failed状态处理：停止后续本轮工作，取消附件处理，不重置查询次数、不改变控制权限。需求整理新认领也不复用维护前时间。时间内正常流程保持原语义。
- 当前证明为受控时钟、真实SQLite与生产运行时测试；80357/bd32bb完整链、类型及打包启动全部通过，可保存本地批次但不能据此部署或宣称真实验收完成。此前未修改的PATH等待失败在隔离复测及新完整链未重现，根因未确定，保留历史证据；实际结果见VERIFY最新记录。

## D-081 — 待核查查询采用独立次数账本，不复用发送重试授权

- schema 28 增加 delivery_queries，每次外部查询前追加 outbox/快照摘要/实例/时限/次数；尝试顺序强制递增至三次、不能删除或改写既有完成证据。查询回执仍留在独立加密 Vault，账本仅固定 outcome，不存传输错误原文。
- 既有非幂等 unknown→dead_letter 和过期发送认领→待核查规则保留。独立 reconcile 只查询，不把 PROCESSING/失联/缺回执改成 confirmed_unsent。没有回执停止一次；未知结果按现有基准退避，三次后保持待核查；中断尝试占预算，过期后只能新建下一次查询。
- 正常发送与核查交替，避免任一工作长期饿死；本轮选择偏好在内存中，不是授权状态，重启不会清除数据库预算。成功后按实际查询耗时校验实例/认领期限和完整 Outbox 快照，原子记录 sent、发送序列及归属选择证据；原发送 attempt 不增加。
- Owner 现有核查清单只读，新的送达状态会使旧清单指纹失效；本批不新增人工补发/确认接口。未来此类操作必须与快照/查询认领协调，不能声称已支持不存在的审批动作。实际钉钉与主机重启仍需真实证据。

## D-080 — 私有受理回执独立加密，恢复入口只查询

- 查询标识具备查询/撤回能力，不混用附件字段，不进入业务 SQLite/审计/模型。复用已配置应用 Secret，使用独立 HKDF 用途及随机盐/IV；文件以去重身份摘要命名、0600、目录0700，认证绑定文件身份，拒绝损坏/符号链接/身份错配。Secret 变更无法读取旧回执时保留不确定，不降级新发送。
- 完整回执无覆盖发布，写入和目录同步后才查询；同身份不同回执拒绝覆盖。仅在 Outbox 原认领保护下发送，私有回执不是新的发送队列，也不取代单实例 fence。发送到持久保存之间仍存在崩溃窗口，必须继续禁止重发。
- 真实运输暴露 query-only reconcile，发送前检查已有回执优先于 session；路由删除不绕过检查，没有历史回执的正常 session 不额外要求主动凭据。绑定 Outbox ID/去重键/app/群/payload/渲染，不能拿其他内容回执证明当前回复。
- 本批不放宽原 unknown→dead_letter、不冒充后台恢复完成。后续独立实现持久核查调度、三次停止、实例及认领时效核验、成功状态原子提交与 Owner 核查/补发防竞争，再做真实平台验收。schema 27 不变。

## D-079 — 主动发送受理后再核对状态，查询失败不能重发

- 根据已核对的官方 groupMessages/send 与 groupMessages/query 契约，processQueryKey 仅证明拿到查询标识，不证明发送完成。普通主动群消息在同次调用内进行一次同 app/同群查询，只有无矛盾 SUCCESS 进入已发送。
- 查询失败发生在发送受理之后，哪怕查询 HTTP 返回 4xx/5xx，也不代表原消息未发。统一保留 unknown，沿既有非幂等运输规则进入待核查；不降级其他通道、不重发、不将 RECALLED 当作未发送授权。
- 查询预算四秒，加原有三次八秒请求不超过默认三十秒认领预算。原共用请求器只允许缩短超时，不允许调用方放大；头/正文/迟到响应处理沿原安全封装。
- 查询标识不落普通日志/错误/模型，不作为引用别名；人员已读列表只随有界响应临时读取，不存储。持久回执、持续查询、远端状态晚到恢复仍未完成，这一阶段仅消除虚假的已发送结论，不能当完整对账验收。

## D-078 — 先证明真实输入通道，禁止用内部附件测试替代平台能力

- 官方消息类型和当前接收/发送指南已实际读取：群内 @机器人不支持文件接收，主动群 API 暂不支持 @，接收表未说明 originalMsgId，processQueryKey 为查询/撤回使用的加密标识但没有到引用字段的转换证据。
- 保留已有防护与内部解析实现，不凭缺失字段删除兼容路径，也不将回执强制当作入站消息 ID。当前自然群聊完整性必须先经过真实事件采集和能力验证。
- 若需要专用个人事件接入，先明确账号、白名单、读取范围和凭据存放，再请求唯一 Owner 授权；禁止自行扩大身份/权限。@文本和文档链接可作为部分路径，不重定义原完整目标。

## D-077 — 先让材料提供者回答具体选择，避免重复索要材料

- 支持“第二份”不代表交互闭环成立；系统需要先列出原序号和文件名，并说明应回复哪条上传消息。仅在单份新正文完整可核验且原材料归属一致时生成这一具体问题，选项展示最多三份，超出提示总数。
- 定向对象由已保存替代消息的 principal/source/quote 走现有身份校验，不从文件名猜人，也不默认事项创建者。文件名脱敏、清理控制符并限制长度，发送端继续转义 Markdown。
- 将泛化“未读取”提示设为选择问题的后继，避免一面已有可读版本、一面又要求重复上传；只调整当前提问顺序，所有材料完整性门禁仍强制执行。有效答案解除选择问题后仍检查未读兄弟文件。
- 没有替代审批、重试或身份授权；边界表达、复杂改选、真实模型和实际钉钉回复引用能力仍独立验收。

## D-076 — 多附件选择沿原上传位置和后续消息来源确认

- 允许原提供者用序号或唯一精确文件名指定同条原消息的未读材料，并允许随后回复替代上传消息补充选择。不按过滤后的附件位置解释“第二份”，也不从附件正文推导替代意图。
- 初始明确选择与后续被识别的选择必须指向同一个原附件；重复名称、冲突和超过 20 条匹配后续消息均不推断。原文件、错误、选择来源和完整新正文保持可追踪，未选中的旧材料继续阻塞。
- 上传时与单独选择后均提供通俗、明确的对应反馈；自然解释尚在排队时也可根据持久来源确认关系，但不能宣称目标确认或代码修改完成。不扩大成员控制权限，不引入 schema 变更。
- 确定性短语仅为当前支持范围，不作为产品唯一沟通协议。多新文件、多有效版本改选、已用事实变更和真实群聊仍须后续实现/验收。

## D-075 — 用带来源的可读材料解除旧附件阻塞，不抹掉历史

- 已确认旧失败附件永远参与门禁，重新上传成功也无法继续。对原提供者、原消息引用、单份未读旧附件和单份完整新材料采用有界确定性关联；不从文件名或附件正文解释替代权限，也不自动退役已经提取使用的事实。
- 关系从已持久化入站消息和实际正文证据重算，双方规范化哈希/附件 ID/来源 ID/内容哈希随模型应用收据固化；指纹漂移沿既有门禁拒绝旧模型结果，无额外 schema 或可变授权开关。旧附件与提取错误保持原样，旧失败通知在发前校验被替代状态。
- 原提供者后来提交唯一有效替代可解除早先他人建议产生的未读门禁；同一原材料出现多个有效替代则不擅自选最新。不承诺已支持多文件选择、任意自然措辞或修改已用材料；这些需独立需求确认/权限路径，不把本批单份来源关联视为完整自然协作验收。

## D-074 — 附件整理的中断也必须留下尝试与通知

- 发现已提取附件的过期投影认领只被新 token 覆盖，服务重复中断时不会计入三次上限。沿用 schema 27 既有不可变失败表和 Owner 恢复窗口，不新增竞争性计数器。
- claim 在原 BEGIN IMMEDIATE 内先结算过期 token，再计算退避和停止状态；普通失败与中断复用内部结算逻辑，通知入队失败则账本及认领整体回滚。用“尚未确认整理结果”描述未知结果，不推断回调完全没有生效。
- 下一次整理仍走既有来源校验/幂等投影；不重复下载、不删除证据，也不改变 Owner 权限。测试覆盖过期后迟到回调、两种失败的累计、重建不重置、首次/第三次通知和已读正文保留；真实进程/群聊另行验收。

## D-073 — 按旧实例、固定 ID 和独立无遗留查询恢复文档资源

- schema 27 在资源创建前绑定运行实例 owner/fence；旧版行保留空归属，迁移不制造删除权限。恢复只查同 context、较旧 fence、已知 ID 的未清理行（每批最多 20 条），不按名称前缀扫描或删除。
- Docker 操作前持久化尝试预算；核对 ID/name/label/固定 image 后才执行 rm，再用独立 container ls 确认该 ID 缺失。失败和三次耗尽保留原始身份与未完成状态，重建对象或服务不重置预算。没有 ID 时不猜测删除；后来入账的固定 ID 可以在后续批次核查。
- headless 在附件后台批次前装配恢复，使用实际主账本、受信任显式 context 和运行时 lease/signal。调用方的 assertActive 使用实时 clock；批次 now 只用于补充账本检查，不能代替实时租约检查。取消/过期必须传播，不能被通用 Docker 异常处理吞掉。
- 本地 SQLite/故障注入不等于真实 Docker 重启证明。未启用现有试点解析器、不更换运行容器；真实镜像隔离 smoke、未知回执、独立 supervisor、三次耗尽的 Owner 恢复和完整主机重启仍待验收。

## D-072 — 先记录文档资源身份，再执行 Docker 副作用

- 原解析器仅在内存中保存随机名称和 ID，finally 无法覆盖服务强杀。增加 schema 26 主账本资源表；reserve/created/cleanupAcknowledged 使用独立短事务连接、FULL 同步，分别位于 create/start/交付结果之前，不保存正文、文件名或能力凭据。
- headless 工厂用实际 databaseFile 装配持久记录，低层受控 smoke 仍可显式不使用账本。容器新增与唯一名称绑定的资源标签，为后续检查提供依据。context 隔离的待核查列表不是自动删除授权，也不能证明原进程已死。
- rm 的业务回执只称 acknowledgement；未实现独立 inspect、实例失效验证、创建迟到及强杀重启回收，保持这些为后续验收。迁移不回填历史归属，旧版本测试夹具同步降级删除新表后验证真实升级，不放宽迁移校验。

## D-071 — 停止传播到解析命令，但不能取消清理

- 协调器保留入账前认领校验，并将生命周期上下文通过配置工厂原样交给解析器。先行回归复现停止前/创建后/解析中/清理中/认领失效均未阻止解析结果；独立 CLI 测试复现 abort 被忽略直到超时。
- 不把 signal 传给 create，等待其有界完成取得 ID 后先校验是否仍可启动，防止普通停止打断创建回执。仅 start 绑定 abort；NodeDockerCommandPort 取消时杀 CLI、等待 close、移除监听再报告固定错误，不泄露取消原因。
- finally 的 rm 独立于已取消信号执行，之后再检查活跃状态；rm 失败优先保留 cleanup_failed。强杀服务/迟到创建/Docker 无响应仍不是已解决场景；下一阶段需要持久化资源所有权和重启回收，不能用本地 abort 测试替代。

## D-070 — 文档解析显式接入后台，配置不代替隔离证据

- 先行测试复现 headless 即使配置固定解析镜像，PDF 仍走 bounded-text 判为不支持。生产工厂注入配置解析器，复用既有来源入账、脱敏、完整性、投影去重和活跃认领校验，不创建第二条正文处理路径。
- 默认关闭，启用开关严格为 0/1；启用要求固定镜像和明确 Docker context，禁止隐式默认环境。健康检查只组装/校验配置，不调用 Docker 或下载。没有“verified=true”替代真实镜像检查，现有试点不变。
- 通过实际 headless 工厂、协调器和 SQLite 加受控下载/Docker 端口覆盖完整、部分、失败、禁用及迟到取消结果；这证明接线和持久证据行为，不是实际 Linux 解析、即时容器取消、真实在线文档授权或群聊验收。

## D-069 — headless 直接控制的回复随控制事务提交

- 初始六类控制回归表明：Outbox 写失败时暂停/取消已提交，其他拒绝决定/审计也已提交；仅保存来源不能解决动作与回复的中断窗口。采用受信任 replyRequested opt-in，动作控制器在原事务内使用规范化事件/事项和原业务文案入队。
- 原 runtime 入队方法保留为历史兼容入口，新请求已在控制器入队后会直接跳过；不更改旧收据或给其补造来源。生产控制权限、版本/候选约束和执行调度不变，普通核心调用不默认生成钉钉消息。
- 共享 commandSummary 从 runtime 等价迁出；批准/退回仍保持 plan 回复，其他控制保留完整当前状态字段。TDD 还复现控制事件与普通输入双向复用，增加持久事件冲突检查，不将已处理事件转换成新任务或控制。

## D-068 — Token 控制、传输收据与文本回复共享原控制器事务

- 原 runtime 先提交控制器动作再另行入队回复；未知 token 每次重放重复拒绝审计，无持久传输来源。三项先行回归复现来源缺失、拒绝不去重及回复失败不能回滚动作。
- 控制器 perform 接受可选请求描述；无描述的既有核心调用不变。prepare 在原事务内检查跨入口事件冲突/不可变摘要，finish 在每条允许/拒绝分支保存收据和文本回复后再提交。重放历史决定仅是返回已发生结果，不产生新授权、动作或发送。
- 摘要包含 token 的哈希、来源类型、群、人员和原因，数据库/日志/回复不保存可执行 token。已拒事件不会因另一条 Owner 消息合法消费同 token 而变成功；身份改写不重新授权。同一 token 的新事件仍由原 token/Owner/版本/有效期规则判定。
- 卡片 payload 未提供已验证来源群，不接收其任意 group 字段，也不从任务猜群。headless 已接原子路径；历史 OwnerCardActionBridge 的独立事件库仅发现测试调用，未改写或迁移，不能以此宣称所有旧适配器已完成原子化。

## D-067 — 状态/刷新回复的存在不能代替原始处理结果

- TDD 复现刷新审批被拒后重放直接 allowed=true/approval_refreshed；旧实现只检查 Outbox 是否存在，没有原始请求/结果记录。新 owner_query 收据复用 schema 25 不可变表，绑定命令/事项/群/稳定发送身份及原因摘要，保存原始 allowed/reason。
- 外层事务覆盖结果收据和 Outbox；现有入队方法改用可嵌套 savepoint，单独调用仍可提交，外层失败则连同 supersession 更新一起回滚。成功刷新与拒绝/状态查询均验证该原子性，不引入外部副作用。
- 无收据的旧回复返回 outcome unavailable，不推断成功、不追加发送或来源。跨普通入站/token 动作拒绝复用查询事件；控制和恢复入口继续使用原哈希/冲突检查。此修复不调整 Owner 权限、身份、生产/合并边界或实际配置。

## D-066 — 直接控制回复不得用目标事项反推请求群

- 六类 WI 文本控制的解析器保留规范化入站 conversationId，控制器在已有不可变 outcome_json 保存来源，拒绝与允许决定均覆盖；schema 25 不变。保留原 payload_hash 算法兼容旧收据，另对已保存来源做精确一致性校验，旧收据重放不能取得新路由权限。
- 发送端先判定控制请求收据，再决定计划类消息路由；否则批准/退回的 plan 回复会被替换成目标事项的最近消息事件，跨群请求可能回错群。查不到来源不猜测，不把 conversationId 等同 openConversationId。
- 控制/审计/来源收据仍使用原控制器事务，运行时回复仍为后续独立事务；不虚称两者已原子合并。状态查询、刷新审批和 token 卡片动作无本批收据来源，需要后续持久协议与故障恢复验证。未新增权限、实际配置或真实发送。

## D-065 — 恢复回复的来源与恢复收据一起持久化

- 生产发送装配的合成测试复现：需求/附件恢复回复已入账但无 external_events 来源，临时 session 丢失后无法备用发送；成功恢复和拒绝恢复均受影响。
- 在现有不可变 recovery_requests.outcome_json 增加可选 conversationId，适配旧收据而不迁移/改写历史；路由读取两类恢复收据，仍只接受单一已配置来源。该字段不参与授权决策、不展示于业务文案。
- 来源与恢复、审计、Outbox 使用原有同一事务；写入故障整体回滚，同一事件改群拒绝。成功入账的回复由 Outbox 幂等控制，发送器重建不重发；旧收据缺来源时不从任务推断，也不通过重放赋予新发送权限。
- 不改变 Owner、自动恢复权限、三次失败边界或 Spec，不代表普通 Owner 命令和卡片回复的来源已补齐。

## D-064 — API 回归的引擎补齐必须由夹具封闭

- 带 claude 名称的测试配置会触发正式 instanceConfigs 默认引擎补齐；仅隔离 HOME 和禁用本地模型 HTTP 发现仍会执行开发机 PATH 上的其他 CLI。时序证据显示创建机器人等待探测而非 Chief 业务判断，三次创建足以超过原 20 秒测试限制。
- 不修改生产默认补齐、不缓存/跳过真实产品健康检查、不提高测试超时。测试从正式装配枚举补齐实例并显式配置为 unknown-driver shadow，仅保留受控 fake Claude，新增整个非 Claude 集合均 unavailable 的断言。
- 原 88 项真实 HTTP API 场景及配置规则测试全部通过；这证明当前夹具中阻塞被解除，不是生产 CLI 性能问题已修复，也不证明六类钉钉真实试点完成。

## D-063 — 多群备用发送必须绑定持久化来源

- 生产装配的受控 fetch 测试复现：原群 session 不可用时，不同群的回复会共用全局目的群，造成潜在跨群内容泄露。
- 明确配置来源群到主动目的群的映射；复用持久化事件/群 alias 和核查收据，不用 payload 或事项内容推断地址。未知/有歧义来源不可主动发送，保留原 session 路径。
- 单群旧配置仅在来源可证实时兼容，多群全局值不作为兜底；配置冲突拒绝而非默选。没有修改真实部署配置或凭据。
- 不把 processQueryKey 当机器人消息 ID，不扩大 Owner 权限，不重置待核查消息，不将路由修复当作送达证明。历史 Owner 命令/卡片动作缺持久化来源群的恢复仍待补齐，不能由目标事项反推请求发生在哪个群。

## D-062 — 核查查询是有界来源快照，不是补发权限

- 专门解析直接、无附件的自然查询短句；复用不可变 collaboration_owner_text_commands 收据并加 kind=delivery_review，不新增 schema。使用 delivery.review 能力但仍只认当前唯一 Owner，无其他管理员。群白名单检查位于 Stream 路由前。
- 查询保留当前群、Owner 代次、源消息摘要、原投递指纹及有限清单；响应标题去敏并抑制 @，不输出投递 ID、错误原文、验收 token 或代码。查询仅新增审计/查询收据/新答复，旧投递和 Work Item 保持不动。
- 未归属回复关联的是 external_events.id，而不是 source_event_id；扩展测试暴露遗漏后按正式内部关联修复。原需求引用只按已持久化同群来源缩小范围，未知引用不退回全群猜测。
- 重放保持原查询结果，Owner 已更换时不释放旧清单；旧查询事件进入普通入站、Owner 文本命令或卡片动作时拒绝。发送前复核 Owner 代次与原投递指纹，已变化则废弃旧通知，不擅自重新查询或补发。
- 本批只做到核查定位，不提供“确认已送达/批准补发”。后续必须将该动作绑定唯一 Owner、具体消息与当前证据，不能清空 dead_letter 或把本地状态当远端证明。

## D-061 — 投递待核查与服务就绪分开，先让问题可见

- 五项先行测试复现健康接口完全没有投递状态：停止消息即使持久保存也无法从健康检查发现。新增只读聚合，沿用 schema 25，不迁移/修改历史记录。
- 统计只包含 source=dingtalk、sent_at/superseded_at 为空且处于 pending/claimed/dead_letter 的记录。当前 claimed 计发送中，过期认领/停止/旧不确定重试计待核查；可信未发送分类前缀的 pending 重试单列。待核查不等于确认失败，空队列不等于历史消息全部送达。
- delivery.status 是独立运营状态，不纳入 ready/reason 或执行许可。否则某条投递异常会通过 health().ready 冻结其他仓库/事项，违背并发协作目标。读取失败计数为 null，不显示伪零，不向健康输出带出 SQL/提供方错误、消息正文、ID、Owner 或群标识。
- 本批复用现有 --health 入口，验证不取得租约或绑定 Owner、不改队列/重复发送。尚未实现唯一 Owner 消息级核查、安全再发送或主动私信告警；不能把本机可见性当成用户已经收到异常通知。

## D-060 — 成功与失败互相矛盾时不得选择性相信其中一项

- 先行 25 项受控测试中 18 失败，复现 session 用 OR 接受冲突字段、主动消息忽略数字错误码/错误类型，以及令牌响应被拒绝仍缓存并实际提交消息的问题。
- 共享业务状态检查只提供否决/分类，不自行证明送达。检查 success 布尔值、errcode 安全整数、code 整数或非空精确字符串；成功与拒绝共存或字段类型不合法返回 unconfirmed，无成功标记的明确拒绝为 rejected，其余 clear 仍由各端点检查所需回执。
- 普通消息 processQueryKey 与失败并存不能当作明确未发送；session 明确且一致的拒绝仍可回退。令牌失败阶段尚未发出消息，不缓存带异常状态的响应，保留安全重试。错误消息仅固定内部分类/数字码，不回传提供方文本或令牌。
- 不猜测 card createAndDeliver 的目标回执格式，不将 processQueryKey 当 originalMsgId。本批验证正常普通消息/会话仍成功、冲突不标送达、重建 dispatcher 不重发；真实卡片与群聊试点仍未验收。

## D-059 — 回复等待必须包含响应正文且不阻塞后续队列

- 两个发送器没有请求期限，且 response.text 在读完后才检查大小，挂起正文会让发送与后续队列一直等待。10 个先行失败测试复现 header/body 挂起、超限正文不断流及迟到响应问题。
- 共用 boundedReplyRequest：每次请求固定 8 秒总期限、主动 AbortSignal 与不依赖 transport 配合的 promise 退出；流式按字节限制，失败/HTTP 拒绝取消正文，不等待可能挂起的取消。迟到 fetch 响应直接丢弃并取消，不缓存令牌、不改已返回结果。
- session 允许显式拒绝后的正常回退；最多 session/令牌/主动三个 8 秒期限，适配当前默认 30 秒认领。使用自定义更短认领期时仍须配置方保证时间匹配，不声称该默认足以证明任意配置或事件循环阻塞。
- 仍沿用 D-058 的提交阶段分类：令牌阶段失败可安全重试，消息阶段不确定只留待核查。未更改 Owner、凭据、权限或群发送配置；未发送真实消息。业务成功冲突字段与卡片缺少具体成功回执仍是独立待核实项，不用本批超时测试掩盖。

## D-058 — 非幂等投递丢回执时不能换通道重发

- 只读查看 dingtalk-chat 的 chat-bot/contracts 说明及实际 `dws chat message send-by-bot --help`，未执行发送/群查询或认证。契约明确 processQueryKey 用于撤回，不是 openMessageId；Bot/Webhook 幂等键能力为 false，因此本地 dedupeKey 不能当作远端去重承诺。机器人引用映射仍缺正式对应证据。
- 现有路由在 session 抛错、5xx 或无法验证响应时会改主动通道重发；Outbox 会重试 unknown，重启后还会重发过期 claimed。五项 TDD 失败复现重复风险后修复。
- 增加 unknown 投递结果与发送前 not_sent 分类；生产 Outbox 显式 only-confirmed-unsent 策略。unknown 进入内部 dead_letter、保留未确认原因和空 sent_at；过期 claimed 同样不重放。旧 pending/attempt>0 无主控安全分类的记录也停止自动重发，不猜它从未送达。
- 安全 retryable 由主控在 last_error 前加固定 delivery_confirmed_unsent 分类标记，与状态/退避同事务保存；只有生产适配器证明未提交或收到明确拒绝才返回该类别，原始提供方错误不得直接自报此标记。重启仍可恢复安全重试，不改原最大次数。默认测试/其他幂等 transport 行为不变。
- 代价是进程在认领后、发送前崩溃时也可能需要核查；无法区分未发送与已发送丢回执时，不能通过自动补发掩盖不确定。此批不自动人工重发、不冒充最终送达，也不等于完成远端引用/真正线上闭环。

## D-057 — 显式引用不能被自然措辞覆盖，乱序通过持久来源续办

- 核对发现未知引用会跌入新建/单候选关键词关联，且归并模型的输入没有引用来源；“第二个”还可能套用另一条已发送选择题。先用 4 个失败测试复现，不用推测的机器人远端消息编号补洞。
- 未解析引用在普通入站保持 ambiguous；后台在模型和序号选择之前检查原引用，无法证明则 clarify。通俗回复明确缺的是被回复消息的归属，零候选不展示不可用的序号操作；原材料不丢失。
- clarify 任务只有在同群原事件已关联且事项非终态时重新入队。已知引用使用持久原事件确定事项，不调用模型猜归属、不受近期候选上限影响；完整当前文本留存，事务复核当前引用关系/认领/事项版本/Spec，并保存引用来源证明。循环、跨群和终态引用不触发续办，重启及重放不重复贡献。
- 发送接口目前只确认业务成功，主动消息使用 processQueryKey；仓库无该字段等于 originalMsgId 的证据。机器人回执映射仍待正式契约/真实样本核对，本批不虚构映射、不增凭据、不发送测试群消息。旧版已被错误关联的数据不自动回写。

## D-056 — 需求整理恢复独立于代码重试，授权不跨失败窗口复用

- 三次需求解释失败后原先仅提示负责人检查，没有可用恢复入口。专用短句由 Stream 分流，经当前实例租约和唯一 Owner 的 work.retry 权限校验；不经过模型判断权限，不直接排队执行代码。
- 通过钉钉会话别名定位同群事项；回复任一原始贡献可缩小选择。多个符合条件的事项先澄清，只恢复选中事项内 failed/3 次/已知解释错误的输入；暂停、终态和有效运行认领不恢复。
- schema 25 在一个事务中归档原失败窗口、重置待整理状态、保存审计/通俗回复/不可变请求结果。保留原消息与 Spec；普通入站、Owner 命令和附件恢复间拒绝事件串流重放，旧授权不跨窗口生效。
- 失败通知使用输入来源、恢复代次和当前 Spec 修订共同去重；新补充改变需求版本后，仍未解决的旧失败可以得到新提醒。投递时再核对当前失败状态，避免恢复后发送旧的停止提醒。通知不是根因修复或代码完成证明。
- 验证以受控模型、合成身份和真实本地数据库为边界；Docker 进程恢复不等于真实钉钉、真实模型或整机重启验收，不启用真实凭据或替换现有试点。

## D-055 — 用固定缓存镜像补真实进程证据，不伪造在线能力

- 公开镜像站和 PyPI 仍 DNS 超时，不能构建或启用 parser；不借用凭据、不改系统网络。现有 Node 固定镜像可用于实际 Linux SQLite/子进程恢复和断言报告器验证，两者证据边界分开。
- 探针将可信打包代码经 stdin 写入容器自有 tmpfs，不挂载用户目录、不放开只读根；非 root、无网络、cap-drop ALL、no-new-privileges、资源上限、无日志驱动均实际 inspect。fork 清空宿主 eval 启动参数，避免继承 bootstrap 而不运行目标阶段。
- 第一阶段持久提交 15 条合成输入、处理一条，再通过 IPC 回执后被 SIGKILL；第二阶段断言剩余 14 条任务、重放防重和全部验收。SQLite 行的空原型在测试比较前转为普通对象，保留全部字段和值断言，不修改生产逻辑。
- 本机 HTTP 定向测试获准并通过后，再实际执行完整 pnpm test；本批无过滤的完整链通过。新增探针有独立真实 Docker 通过和最终编译证据。只清理自建临时资源，旧服务和历史容器不动。
- 合成输入、受控模型必须在输出中明确标记；不能将这一结果写成真实钉钉、真实文档或主机重启完成。目标保持完整，不因局部基础设施通过而缩小验收。

## D-054 — 持久 Spec 承接已处理历史，积压发言逐条整理

- 原实现将消息截到 2,000 字符，并只读最近 12 条；历史总数超过 12 时永久添加无法解除的 context-incomplete。新消息还会 supersede 前一个 pending 任务，造成未解释输入被跳过。以长消息尾部要求、15 条连续/突发补充测试复现后修复。
- 只认可应用成功且 result_revision 不晚于当前 Spec 的源匹配收据。收据由主控写入已冻结的 proposal_json，绑定 sourceEventId、principalId、normalized_json 摘要；模型不能自报覆盖证明。源被改写后必须重新展示，旧无收据不推算为已覆盖。
- 当前消息必读全，保留原引用及待确认人来源；历史模型输入上限 12 条 / 24,000 字符。不再用历史总条数判断缺失：存在未覆盖且未展示的输入才保留完整性门禁。后台流式核对原记录，不向模型无界灌入聊天原文。
- 新输入不覆盖旧 pending；按入队时间和源事件插入顺序处理，时间相同也不能用文本 ID 排序。同一事项已有活动认领时不能再解释另一条；Spec 变化后重试原输入，而不是丢弃。最后一个积压输入整理成功前保留等待门禁，任何失败输入仍阻塞。
- 较早输入三次失败也要通知，不能仅检查最后一条群消息。保留失败并停止，提示负责人检查；不声称重新发一句话即可清除失败。历史已替代任务的补偿、解释失败后的 Owner 恢复协议仍需另行完成，不能擅自重开三次停止。
- 引用机器人回复检查确认当前只有入站来源关联，发送端没有回执映射；本批没有伪造支持。真实模型/群聊、全仓回归、Docker 六类试点继续未验收。

## D-053 — 将附件投影恢复与代码执行重试分离

- 唯一 Owner 通过独立短句发起恢复，不复用会进入执行队列的任务重试。审批身份来自稳定企业/人员标识，不采信显示名或管理员字段。
- schema 24 以附件最新失败序号为不可变授权边界，后续失败窗口重新计数；旧事件重放只返回既有结果，改写事件不得跨入新任务或 Owner 命令路径。恢复、审计、请求摘要和通俗回复同事务保存，持久化失败整体回滚。
- 查询必须先将钉钉外部群标识映射到 Ledger 内部会话标识。直接使用内部标识的夹具曾掩盖这一缺陷，改为不同标识后四项测试复现，现按正式映射查询。
- 多附件不猜选，按原消息和一基序号定向；拒绝暂停、取消、完成、正在处理或跨群对象。正文和来源不变，历史三次失败保留。恢复仅重开整理，不调度代码、不承诺修复成功。
- 本批未使用真实凭据/群消息、在线文档或 Docker；全仓与端口模型测试继续保留为未验证。

## D-052 — 需求投影失败与下载失败分开，保留正文并按认领收束

- schema 23 新增不可变投影失败收据，绑定每次新生成的 claim_token，记录固定安全分类和 retry_after；不将可能含 Secret 的原异常持久化。常见来源/完整性错误有固定分类，其他回调错误归入 projection_unavailable，三次同类分类触发停止，不宣称已证明原始根因完全相同。
- 认领时在事务内重查退避/三次门禁，完成或失败时重新校验当前认领与实例活动性。失败释放认领的 owner/expiry 必须同时置空，保持既有数据库成对非空不变量；退避单独保存在不可变收据中，不能借租约 expiry 表示重试时间。
- 失败和首次/终态业务反馈同事务入账，出站失败整体回滚；提取证据验证也放进受控失败路径。保留 ready/原正文/来源，不把投影错误变成下载失败或重新下载。界面说明提取记录已保留但需求整理未确认；投递前核对最新失败、当前投影/事项版本和控制状态。
- 查询在 LIMIT 50 前排除停止和退避记录，50 个停止事项不能使后面的健康附件永久无法投影。取消或另一个认领取代后，旧回调不写失败/成功记录。三次后的预算重开必须另有唯一 Owner 受控恢复协议，本批不通过重启、补充消息或换 coordinator 隐式解除。
- 本机默认 loopback 预检明确 EPERM，未重复申请已超时的全仓授权；默认权限运行不需端口的相关回归，明确排除 natural-intake-model.test.ts。70 文件 / 594 项、类型和服务端编译通过，不代表全仓或真实试点通过。

## D-051 — 用生产装配的组合故障验证附件 ACK 边界

- 不能仅以独立的 fake persist、Vault 或 Stream 单测推断整个入站顺序成立。将生产 Stream adapter、runtime、Ledger、Vault 和 coordinator 串联，在 SDK acknowledge 时反查持久事件和可解密能力，断言后台尚未开始下载。
- 故障场景覆盖 Vault 第一次保存失败、SDK ACK 失败、普通重放与确认后立即重启。恢复不重复建事项/附件/回复，挂起下载只启动一次，跨默认租约时长维护仍健康并投递已有回复；停止后迟到下载不能入账。
- 入账采用 normalizer 的真实 receivedAt，不使用消息中的历史 createAt。初版测试将维护时钟固定到 2023 年，导致 2026 年入账附件尚未到 next_attempt_at；按源码证据对齐接收/维护时钟，保留下载次数和到期断言。此为测试夹具修正，不宣称发现或修复产品时钟缺陷。
- 本批业务代码不变。完整 pnpm test 的权限检查原请求及一次重试均超时，命令未启动，不算测试失败或通过；不继续重试该权限请求。默认权限下组合及相关四文件 73 项、类型、服务端编译通过。全仓和真实试点证明仍待补齐。

## D-050 — 附件读取失败按持久证据停止，反馈不能脱离来源和当前状态

- schema 22 新增不可变 attachment/attempt 失败收据，已确认失败按最近三次相同安全错误码判定停止；attempt_count 可能含崩溃认领，不能直接当作同类失败次数。升级保留现有记录、不推算历史失败；未知上游异常折叠成固定安全码，禁止持久化原异常/下载码。
- 下载/能力读取/文件保存的失败记录、pending/failed 更新和首次/终态回复在同一事务内提交，并再次校验租约和当前尝试；出站持久化失败时整体回滚，保留能力供旧 claim 超时后恢复。永久拒绝、完整性失败、未支持格式的终态漏发可由维护补回；不会为发通知重做已终止的读取。
- 首次重试和最终停止使用简练中文，不要求编号/命令、不将未读附件说成已修改。采用已有文本消息，不依赖 cardTemplateId。回复绑定原附件消息 session，不能使用同事项的最新消息替代原来源。投递前校验附件当前状态/尝试和事项版本/active；过期提示 superseded，不承诺撤回已在途消息。
- 作用范围是附件读取失败，不宣称投影回调异常、任意存储故障或未知解析进程也已实现三次收束。真实 session 过期后的主动路由、完整回归及六类真实试点仍需验证。相关 71 文件 / 592 项、类型及服务端编译通过，未部署。

## D-049 — 同轮补充测试以输入事件同步，不依赖短暂时间窗口

- 上一轮全仓 steer-e2e 断言 steered=true 失败，而原源码定向和完整两个文件均可通过；审查发现假 CLI 的 slow 模式固定 800 ms 后结束，测试只观察过 busy/tool chip，无法保证发出补充时仍在同一轮。增加 1,200 ms 观察延迟后复现相同断言失败，证明这一时序假设脆弱；不据此断言上一轮所有失败都同因。
- E2E 专用 wait-for-steer 模式改为收到真实 stdin 补充消息才产出含补充的一次回复。保留 delayed observer、202/steered、转录顺序和单次回复原断言，保留既有测试时限；没有补充、没有结果或错误合并仍失败。原 slow 模式保留给其他驱动测试，不修改产品代码、权限或队列行为。
- Chief 分区用例在原源码完整两个文件 90 项中通过，历史超时原因仍未证实；不能以本夹具修复宣称消除该问题。定点修复须由新的完整 pnpm test 再验证，有限通过次数不保证永久不再抖动。

## D-048 — 先解除附件读取对群聊响应的阻塞，再修复历史投影

- 2026-09-06 实现更新：入站只执行持久化，下载由下一次维护启动，防止后台 Promise 微任务先于 Stream ACK 开始下载。维护单批执行且不等待附件结束，能力持久化失败仍拒绝确认。headless 将同一捕获的取消信号和实时租约 guard 传给摄取及证据回调。
- 下载默认总时限 15 秒（构造选项可设 1–120000 ms），每个 fetch/reader await 响应取消，即使受控 transport 忽略信号也有界返回；取消后的 token 不缓存，不继续解析地址，迟到 Response 取消 body。超长响应/HTTP 错误释放 body，reader.cancel 本身挂起时不阻塞返回。该机制不证明自定义 transport 或容器进程已经终止。
- 异步下载/提取返回后核对当前尝试，成功/失败提取事务内重新核对租约和 claim；停止/失租拒绝证据回调。关机等待真实后台 Promise，有界等待未结束时保留租约、拒绝原对象重启，记录 attachment unsettled；复杂解析进程未接入终止协议，不宣称强清理完成。异常后台处理目前仅安全日志与后续维护恢复，连续三次失败停止及面向用户的持久反馈仍需后续实现。
- 本批 71 文件 / 586 项相关回归、类型和服务端编译通过，另补运行时挂起附件与 Outbox 同行验证 20 项通过。下方为原设计形成证据，不再表示本批尚未开始；这些本地受控证据不替代真实群聊、在线文档、模型或 Docker 验收。

- 本轮只读审查确认：Stream adapter 在确认消息前等待 ingestAttachments；runtime 直接等待 process，维护 drain 同样等待 process，而 drainPromise 使后续维护复用同一等待。默认实例租约 30 秒，下载器取 token/解析下载地址/读取正文均没有明确 timeout/AbortSignal，流式 reader.read 也可能长时间等待。因此历史投影修复不是下一批最高优先级，先完成附件摄取生命周期和及时确认。
- 拟拆分“校验已注册来源并持久化私有能力”与“后台下载/提取/投影”：前者完成才确认，后者不占用维护循环。不得为快速确认而先 ACK 后存 vault，也不在消息/数据库/日志暴露下载能力。
- 背景化不能简单 Promise.race 后丢弃工作。须贯通可取消网络读取、有限并发、退出等待和当前实例租约/附件尝试 fencing；每次异步返回与提交事务前核对，迟到结果不得写 Spec、入队回复或启动执行。原始 claim 更新不成功时不能继续写 extraction/evidence，相关失败需先补测试。
- 验证计划：受控挂起下载时仍能确认已持久化事件、续租并回复另一条消息；能力持久化失败不确认；同附件重放/维护不重复下载；超时回到可恢复状态；停止、失租、新实例接管后旧结果无副作用；随后做完整钉钉链路回归。这里是根据代码证据确定的下一阶段设计，不是已完成实现或真实群试点证明。

## D-047 — 正文尾部必须保存，不能把展示长度当作需求完整性

- 原实现每个 8,000 字 chunk 只投影前 2,000 字，虽然完整性门禁会拦截，但常见稍长文档永远无法继续。改为每条不超过 2,000 字的完整源片段，保留原短文本格式；长片段增加文件、原行和 chunk/片段编号，结尾标记保护切分边界空白。不是增大模型上下文或把未读部分当已读。
- 完整性从持久正文重新生成每个预期片段，对照当前 Spec 事实；保留 12 chunk/100 事实和模型上下文容量限制，任何遗漏继续阻塞，正文中的控制指令不变成权限。投影及既有重放去重协议不改变。
- 原始长行切块也须避开 surrogate pair，防止 SQLite/UTF-8 把半个表情替换为乱码，而哈希仍错误地看似一致。新链路验证精确尾部内容而非只验证哈希形状。
- 本批不迁移已投影的历史附件、不把重复事件用于覆盖当前 Spec。旧截断/旧乱码的受控修复需另有带版本和来源证据的协议。未授权真实文件处理、在线文档或 Docker 启用，六类真实试点仍待完成。

## D-046 — 文档拒绝与超时必须有具体进程证据

- `attachment_document_extraction_failed` 是生产适配器的安全错误边界，不足以证明解析器按预期拒绝文件。验证器须先确认容器已通过隔离检查，再核对实际进程退出码 2、非 OOM/无启动错误、固定错误 JSON；创建、检查、启动及其他异常不得计为场景通过。
- 超时场景注入固定可信 sleep 脚本，须观察 Docker CLI 的确超时且该容器实际仍在运行，随后确认生产适配器已移除容器。每个场景的清理单独核查，最终补偿清理不掩盖适配器缺陷。
- 创建前登记本次随机容器名，回执丢失可按精确名称查询；补偿仅作用于本次名称/ID。清理一个失败仍尝试其余，本轮查询不可用或删除不实则拒绝通过报告。进程被外部终止及迟到创建仍需持久 supervisor，不能凭这个脚本承诺全局无遗留。
- 固定七场景身份，区分 controlled_docker_port 与 docker 证据来源。测试夹具均为自生成非敏感内容；不在宿主机降级解析真实附件，不修改网络/身份或用当前局部测试替代 Docker、在线文档和真实群验收。

## D-045 — 恢复通知必须在开始投递时仍然真实

- 新增窄范围投递门禁，只识别控制面生成的 lifecycle-recovery 来源；不把历史命令回执一律按版本丢弃。比较 session 所属事项、当前版本/计划/最新快照、active 状态及对应结算；blocked 已解除或 recovered 后又有同仓库活动时不发旧提示。门禁同样应用于重试与过期 claim 接管。
- 通知入队改用 plan 关联，使 headless 从实际任务消息选择回复 session；旧 work_item 类型合成恢复事件也解析到任务消息，保留原始去重键。通过实际发送装配和受控 fetch 验证，不新增 cardTemplateId 或读取真实凭据。
- Outbox 判断完成到外部服务接收之间不存在跨系统事务。本批只承诺开始 transport 前的状态校验，主动发送等待 token/已在途请求的最终检查、无 session 的可恢复路由以及多群主动路由仍需独立设计和验证，不能声称任何时刻都可撤回旧消息。

## D-044 — 恢复后台化，但不把退出检查变成自动重做

- startup 的 legacy 扫描排除已有 execution session 的 Run，交由进入 running 后的一次性被动恢复；未持久化旧 Run 的原恢复协议不在本批重写。按 canonical repository 分组，至多四组并发、同组串行，每项五秒超时后停止本次检查。只有被动 authority 读取可放弃等待，绝不能用此取消方法冒充终止 Agent/测试进程。
- 所有异步等待绑定本次实例、账本和 abort lifetime；关机取消并有界等待，迟到权威读取没有写入和启动工作入口。超时/未知保留占用，定向到事项的业务通知仅在当前 Spec、计划及 active 控制状态下入队，按 session/结果去重；普通用户不见内部编号和状态码。
- 恢复释放占用后按原完成门禁检查当前候选，重新播种该仓库复核及真正没启动的待办；候选路径比较使用 canonical key，避免 /var 与 /private/var 等符号链接导致漏排。孤立 execution session 计入尝试预算，且不能被 never-started 扫描当作可自动重做。
- settlement 与 Outbox 不在同一事务，故启动还扫描带 recoveredBy 收据且没有恢复通知的结算，修复崩溃造成的投递缺口；不重复检查已确认的进程，也不重复结算。未通过 Spec/暂停/终态门禁的提示不发送，收据仍保留。
- 该流程不改变 Owner 权限、旧 Run 状态或候选证据，不生产部署、不调用真实模型/钉钉。仍需补全 legacy 运行隔离、已退出但 running 的运行状态收束、通知投递时的新鲜度门禁及真实 Docker 六场景。

## D-043 — 恢复同时需要“协调已结束”与“进程已退出”

- 空进程快照不能证明旧协调器不会稍后启动已预留命令，也不能证明未纳入 Agent 隔离的原生工作目录准备已经结束。schema 21 因此追加 execution/verification finalization intent，仅在原协调返回后的 settle 入口、原有效租约下登记固定命令数；触发器禁止改删标记或继续插入命令/证明。
- 新实例的被动恢复重验原始 owner/fence/run 绑定、独立隔离证明和 empty fingerprint；每次 await 后及最终事务重新检查当前租约、账本和取消信号。并发调用只产生一条不可变结算，不能改变 Run/候选/Meta 状态、审批或尝试次数。
- 零命令复核的例外仅依赖原实例租约失效：后续任何测试命令仍须用旧租约预留，必然被挡。零命令执行没有收束标记时仍保留占用；缺证明、未知或活进程、被拒证明、身份不匹配、取消、关库及租约丢失均不能解锁。
- 升级不为 v19/v20 历史记录伪造收束标记。恢复函数只释放已证实结束的占用，不执行 kill/delete/retry。后台一次性恢复调度、限时关机及幂等中文提醒留待下一批接入；不能将独立函数测试冒充实际服务重启恢复。

## D-042 — 执行终态不是释放仓库的证据

- schema 20 在 prepare 前以 BEGIN IMMEDIATE 检查修改/复核未结算记录并插入 execution session；所有底层执行入口必须经过此处，和复核器对执行占用的检查形成同一 SQLite 互斥边界。固定 run id、base SHA、事项/计划、attempt 和实例 fence，不用可过期的内存锁代替恢复证据。
- 每次 Agent/自测调用先记 command，经过独立注册校验再保存 proof；结束时逐个重验 binding 和 empty fingerprint，并在原有效 lease 下插入不可变 settlement。未知/缺证明/清理异常均保留占用，哪怕旧 Run 已写入失败终态也不能放行新执行或最终完成。
- 纯配置缺失、未进入 runner 的步骤不虚构进程。prepare 返回普通已收束错误时零命令可结算；CommandCleanupError 保留预留。没有证明的 Agent 启动拒绝仍须人工检查，不冒充已确认未启动。
- 迁移新增表及约束，不自动推断旧版本进程已退出，也不自动解锁遗留 session。本批不部署 schema 20；跨版本恢复协议与真实进程终止/主机重启另行验收。

## D-041 — 持久复核生命周期属于复核器，不属于可绕过的运行时包装

- 保留 schema 19 作为唯一复核状态，预留/命令/证明/结算迁入 CandidateVerificationCoordinator。headless 只负责运行状态、取消与未收束降级，不再重复预留；直接复核同样不能跳过租约和账本检查。
- 各阶段写入事务重新校验当前租约，旧实例不能在失去租约后写入 Verifier、Meta 或失败记录。结算仍重验进程为空；缺证明/清理未知/数据库关闭保留占用，不能通过 lease expiry 自动清除。
- 不把 runner 未配置当作曾启动未知进程：它沿用配置失败路径且不预留命令，零命令复核可在原有效租约下结算；实际进入 runner 后缺少证明则不结算。
- 跨进程证据用真实 Node 子进程、同一 SQLite 与同一有效实例身份尝试竞争，确保不是仅因身份不同而被拒绝。当前只证明重复复核被挡；执行准备和自测的持久隔离、原子互斥与受控恢复下一批继续。

## D-040 — 直接 runtime 执行不得绕过仓库队列

- 公共 executeCurrentPlan 在第一个异步 prepare 前同步取得仓库占用，并标记正在处理的事项；忙时明确拒绝，不创建额外工作目录或消耗 Provider 运行。自动调度使用仅允许已持槽调用的私有执行方法，复核期间保留原槽。
- 直接执行结束才释放占用，随后处理同仓库队列；停止或生命周期变化时不再从 finally 启动新工作。其他仓库不受该进程内占用影响。
- 两种方向的 prepare 重叠必须由测试复现后修复；混合复核/修改、Owner 显式重试和停止队列独立验证。进程内保护不替代持久预留，更不允许清除无法确认退出的旧复核记录。

## D-039 — 启动只排队，复核与修改共享仓库调度

- 先连接钉钉并进入 running，再后台执行持久候选的复核；复核等待不能阻塞 start 返回和调用方后续维护循环。沿用可取消、带生命周期 fence 的复核跟踪，不裸启动无法收束的任务。
- 独立复核也占用进程内 canonical repository 队列，释放后优先处理等待的复核，再处理修改；不同仓库可同时启动。持久未结算记录与内存占用用途不同，前者不能靠队列释放或租约过期解除。
- 启动队列一次播种，维护只消费现有队列；实际启动即出队，普通失败需要原有显式重试流程，不每个 tick 重扫失败候选。队列项不再有候选时仅移除，不能顺势触发未经自动执行配置允许的新修改。
- 本批测试使用临时 Git/SQLite 与受控 runner、Outbox，不宣称真实 Stream 或 Docker 重启成功；全入口互斥、混合队列压力和遗留 session 安全恢复继续单独验收。

## D-038 — 未结算复核是持久仓库占用，不随实例租约过期而消失

- 用 schema 19 不可变记录区分“已预留复核”“已进入测试 runner”“已登记隔离证明”“已确认全部进程为空”。每次登记与当前实例 fence 校验同事务，存固定候选/计划/快照与 canonical repository，不用内存 promise 代替恢复证据。
- 结算独立重验隔离证明和 empty fingerprint；任何命令缺证明或清理不明均不结算。零命令且原实例仍有有效租约可安全结算；原实例退出后留下的零命令记录尚不自动解除。
- headless 的执行/调度入口及最终完成门禁查询未结算仓库；启动扫描跳过该仓库，其他群消息仍可进入。原 session 只能由原有效 fence 结算，尚未建立新实例/Owner 的受控恢复协议，不能宣称重启自动续跑完成。
- 此批不部署 schema、不修改真实身份或凭据；证明入库沿用已有隔离 HMAC receipt，不是群里可执行的审批 token。实际 Docker 取消证据仍对应上一批实现，不冒充本批持久恢复实测。

## D-037 — Docker 取消必须以本次容器退出为证据

- 取消信号传至 runner，但 create/start 的 Docker 控制命令继续等待有界结果，避免丢失创建回执或未知是否已启动；取得完整 ID 后在每个启动边界检查取消。登记与 wait 可取消等待，finally 负责身份核查和退出确认。
- 清理同时核对完整 ID、binding 哈希、托管标签和 host generation；只对本次已确认容器执行 kill，重查严格布尔 Running=false 才认定退出。清理未确认保留交换目录并抛专用错误；创建结果不明不猜容器名，不回显 Docker stderr。正常历史容器不自动 rm。
- runtime 收到 verifier 清理未确认后降级、不释放租约且禁止同一对象重启；这只是进程内保护，后续必须持久化运行/隔离和恢复记录才能证明跨进程安全。当前不后台化启动复核、不部署服务。
- 真实 smoke 用固定缓存镜像、自建父子进程和子进程持续写入证明运行中取消效果；同时检查启动门前取消不执行。仅清理本次创建且已退出的两个容器，原试点和历史容器不动。

## D-036 — 取消复核等待，不能冒充终止执行进程

- 外部取消只结束模型/验证协调流程，不把取消记成测试失败；不可变尝试继续保留，取消后不写收据、不启动独立模型或下一测试命令。超时后忽略取消的模型迟到返回同样无后续调用。
- runtime 关闭前发取消信号，后台 Owner retry 捕获异常；未收束复核阻止同一对象重启，关机超时不释放租约。当前任务跟踪仅在内存，不当作跨实例隔离证明。
- 已进入命令 runner 后不打断隔离登记握手，避免取消导致容器停留于启动门前；命令真实终止仍由 runner/containment 负责。本批不将未实现的进程取消、持久 verifier recovery 或 startup 后台化说成完成。

## D-035 — 复核未完成不等于执行失败，通知必须绑定当前任务

- 等待核对、复核受阻和执行未完成分开展示；映射缺失/不可用说明各自原因，不把等待说成配置失败，不承诺尚未实现的自动续办。三类 session 回复隐藏候选代码、路径、SHA 和内部任务/状态字段。
- Outbox 入队与实例/计划/Spec/控制状态/最新尝试/验证契约检查同事务完成。沿用失败事件键，pending 独立去重；旧租约静默放弃通知，其他数据库异常继续暴露。
- 不把 runtime.start 简单改为 fire-and-forget：关闭流程尚未跟踪模型复核任务，必须先验证后台任务、租约、异常和关闭生命周期，再解决重启长等待。文案和迟到通知修复不是及时进度或持久续办已完成的证明。

## D-034 — 显式模型配置与最终收据复核

- headless 默认不开启验收映射。启用必须明确提供两阶段模型名、HTTPS 地址、绝对凭据文件引用和复核规则版本；不从自然解释或 OPENAI/Codex 配置回退。构造客户端不读取凭据、不发请求，真正调用沿用受限文件读取和无工具/无历史/不存储的 Responses 适配器。
- 两端分别构造上下文，可显式使用同一型号；有效 policyId 哈希包含两端模型/地址/凭据引用/规则版本，任一配置变化自动使旧策略缓存失效。文件路径不变但权限或身份变更时仍需要操作者更新规则版本，不能把密钥内容纳入日志或策略身份。
- 最终完成判定遇到自动映射时，必须找到最新不可变请求/双阶段收据，重算绑定并匹配当前 Spec、候选、条件及两套测试使用的契约。只有 passed 标记或伪造 receipt 引用不得通过；不只检查先前算出的覆盖列表。
- 追加状态变化验证：映射等待期间已持久化的暂停/取消、需求更新、候选 HEAD 或工作区变化均不启动后续测试。这些测试不是一次真实 Owner 群聊审批，不替代已有 Owner 传输权限测试或六类试点。
- 按 docker-helper 的配置/挂载边界增加显式 Compose 叠加模板：基础模板不启用模型，叠加模板要求三份明确引用且只读、不自动创建主机路径。只读核对现有服务健康，没有改容器、模型凭据或服务配置。模板解析测试不能证明真实 Compose 合并/部署成功。


## D-033 — 自动映射是有来源的双阶段建议，不是完成证据

- 从固定 candidate commit 的 Git blob 读取受信任命令选定的测试文件，不读取可变工作区内容；禁用 Git 替换对象和按需远程取对象。拒绝符号链接、非普通文件、非 UTF-8、过大/缺失文件、路径越界和非 Node 显式文件命令。输入整体有界且源码脱敏。
- 提议必须逐条覆盖当前验收条件，引用对应文件的完整连续行及确切用例名；独立复核使用另一无历史模型上下文，核对提议哈希、全部条件和 covered/missing/uncertain。任何不确定/遗漏都不产生可用契约，不接受模型额外控制字段。静态行匹配仅证明来源，不证明断言语义正确。
- schema 18 用不可变 attempts/results 保存安全请求及双阶段收据。调用前持久化尝试；90 秒超时、120 秒认领窗口、最多三次，重启不重置预算。迟到结果不能覆盖后续认领。映射策略 policyId 变化形成新验证契约，调用方更换模型/规则时必须同步更新该身份。
- CandidateVerificationCoordinator 可使用注入的映射端口生成局部命令契约，映射后重查当前 Spec/候选再执行；Meta 必须继续检查自测及独立复测断言。reporter 可以先不配置条件绑定而采集自测结果，这不等于任何业务条件已通过。
- 尚未装配 headless 的真实映射模型配置；不借用自然解释或 Codex 登录凭据、不自动开启付费调用。独立上下文不能消除模型共因错误，也不能证明测试未被弱化。自动补测试、真实模型评测及最终运行时状态漂移专测继续待做。


## D-032 — 从 Node 测试框架事件采集结果，报告器不由候选生成

- 受信任命令可选 assertionReporter=node-test-v1，仍须声明 assertionContract。只支持显式 `node --test <relative-files>`；拒绝自定义 reporter、预加载参数和关闭进程隔离参数，不把测试日志当报告。
- Docker runner 把版本内置报告器放入本次交换目录，只读挂载到独立位置；拒绝 NODE_OPTIONS/NODE_PATH/NODE_TEST_CONTEXT 覆盖。命令 attestation 必须声明实际启用该报告器，否则质量门禁拒绝，报告器模式纳入验证契约哈希。
- 断言 ID 由工作区相对文件名和用例名称生成，不随候选工作区改变；同文件同名拒绝以免合并不同结果。只统计实际用例，suite/file 外壳成功不作业务通过证据；错误正文、stdout 和原始名称不进入结构化报告。
- 在缓存固定 Node 24 镜像中完成三类真实 smoke，并检查报告器写保护、日志伪报告忽略和一次性容器清理。按 docker-helper 检查运行状态和挂载，未重建/暂停现有服务，也未拉镜像。
- 该设计改善普通 stdout 伪报告和意外误报，不宣称同 UID 恶意测试进程无法通过 /proc、信号或其他旁路干扰父进程，也不证明用例语义正确。进一步的验证权限分离与自动 Spec-to-case 独立核对仍在 Goal 范围内。


## D-031 — 业务验收绑定断言，不以命令成功替代需求证据

- 验证契约升级为 v2（不是数据库 schema 升级）；可信命令配置声明精确验收条件哈希及断言 ID。绑定变化使旧复核失效并形成独立失败预算，不再用命令名称或参数子串推断业务覆盖。
- 报告严格限定结构和大小，绑定本次执行 run/nonce。开发自测与独立 Verifier 都必须逐条覆盖；Meta 与后续完成门禁重算覆盖，不接受旧 v1 或只写 passed 的裸 v2 记录。保留账本不可变证据约束。
- 任一选定命令失败、重复命令或已报告断言失败均不允许以其他局部通过掩盖。所需用例 missing/failed/skipped 不算通过。低风险无需增加审批，证据不足只报告尚未完成。
- 这是证据完整性门禁，不是语义正确性证明：候选代码仍可能输出虚假报告，测试也可能未正确表达需求。可信 reporter 适配、自动 Spec-to-case 映射及独立语义核对仍须补齐；不要求业务用户配置内部协议。
- 旧命令无 assertionContract/report 时不能再完成业务验收；因此未直接部署试点或修改真实配置。策略测试中的人工 review 是明确标注的合成夹具，不是实际业务执行证明。


## D-030 — 序号指代绑定实际送达内容，归并不能丢掉原需求

- 在 Outbox 发送成功事务中保存归属卡原始载荷、实际完成时间和单调发送序号；同毫秒多次发送不能靠时间戳猜先后，业务失败不生成已展示证据。
- 确定性序号选择只消费同群同人、30 分钟内唯一未解决提示；有后续机器人提问就澄清。当前候选排序、模型自信度和附件中的指令都不能替代该证据。
- 原需求和选择答复原子关联并分别持久排队投影；原始外部事件保留，选择收据含提示、选项位置及选择事件。模型失效或重启不能使原始需求丢失，重复输入不重复记账。
- 无稳定的钉钉出站消息 ID 时不伪造引用链；对跨人代答、多个问题、长间隔和非序号选择继续保守澄清，后续补齐而非宣称自然语言全覆盖。Owner 控制边界不变。
- 异步失败通知必须在写入事务内再核对当前实例/计划/Spec/尝试，不能用旧失败覆盖已取消或更新的任务。


## D-029 — 准备失败必须区分已终结与无法确认退出

- 已终结的准备失败保留当前事项版本及尝试上限；仅后续 Owner retry 控制事件可授权下一次预留，普通消息、resume 或旧 retry 均不能替代。授权和调度间重启可以恢复，次数不重置。
- 已存在 run 的尝试不得写成准备失败；旧实例仅写 interrupted，进程清理错误写 unsettled，两者不因 Owner 普通重试而直接放行。具体解除须后续持久化清理证明，不扩大权限。
- 准备结果与简练通知同事务；不可更新/删除，仅使用固定状态和文字，不把原始命令错误、路径、凭据写入通知。实际执行 run 的恢复沿用原机制，不借新表声明其已安全终止。
- 版本 15 升级通过一次性新增表完成；测试仅在可丢弃数据库重建前版差异，不操作用户数据库。schema 16 尚未部署真实试点。


## D-028 — 重建未启动任务，不自动重试执行状态不明的任务

- 后台只恢复当前已发布计划、最新且完整版本链的 Spec、未暂停执行节点、无当前计划 run/dispatch 的任务；扫描限额之前过滤过期和超预算项。
- 在工作区准备之前通过当前实例租约校验并预留不可变 dispatch，跨重启计入尝试上限。预留后崩溃宁可停住，不能把未知是否启动的任务当成从未尝试。
- pause/resume/retry 不修改业务 Spec，但必须由不可变控制事件解释每个中间版本；普通贡献未投影不能借此执行旧需求。
- 仓库调度键规范化物理路径；服务不健康或退出时不派发新任务。未实现预留孤儿的自动通知和专用 Owner 恢复入口，不宣称普通“重试”已覆盖所有准备失败。


## D-027 — 解析依赖锁定与本地测试不能替代容器验证

- Word 表格记录行和单元格来源，嵌套文本框只读取其自身段落一次，图形未识别部分仍保留完整性警告。新增测试先证实定位缺失/重复，再修复，不以单纯文本非空当作准确解析。

- 公开 PyPI 确认并锁定 pypdf 6.17.0、openpyxl 3.1.5、defusedxml 0.7.1、et_xmlfile 2.0.0 的 wheel SHA256；仅使用二进制发行包，不安装未锁定传递依赖。
- Dockerfile 要求调用方提供经过核对的固定 Python 基础镜像；构建依赖层和运行层分离，非 root 运行，测试目标另行复制测试文件。最终服务适配器继续要求固定镜像 ID。
- Colima Docker Hub DNS 超时、宿主机 Docker manifest 请求超时，备用 GitHub 运行时查询也失败；不为此修改运行服务、全局 DNS 或借用其他身份。镜像尚未构建，真实隔离无新增证据。
- 仅在临时 venv 运行自生成夹具，本地 Python 11 项通过；修复 Word fldSimple/fldChar 未报告部分内容的问题。该路径不接收真实用户文件，不是生产或试点运行时回退。
- 已验证的解析源码可以保存本地提交，但未构建 Dockerfile 只能作为待实测构建入口。真实容器资源/网络/清理、长文档语义覆盖和群聊试点仍列为未完成。

## D-026 — 账本完整性与正文来源优先于回调声明

- 根级 truncated/warnings 必须传播到通知；投影核对全部片段、顺序、位置、哈希和完整性，不接受片段子集。元数据不完整的旧记录失败关闭，测试夹具补齐来源而不是放宽校验。
- Spec 写入时重新生成三类系统门禁，用户和模型不能通过确认或清空问题绕过。CSV 公式状字符串保留为数据，其已知提示不单独表示缺失；其他未知警告保守阻塞。
- 自然解释输入增加同事项附件正文（最多 48 KiB，省略明确标记）；验收可以引用正文原句，但目标确认和问题答案仍须引用当前消息。不可变解释收据保留实际传入来源和片段哈希；附件指纹变化则拒绝旧结果。没有新增模型工具或权限。
- 读取未终结时不认领模型任务；原持久化任务在读取完成后自动继续，不伪造消息、不重置失败预算。不依赖用户另发催促。
- 当前摘要仍有片段和字数上限，超限明确阻塞；分段语义覆盖、真实 Python 解析镜像和真实模型/群聊效果仍待验证，不把受控解释器通过当最终闭环。

## D-025 — 复杂解析器先验证隔离与完整性，再启用

- 新增可注入异步提取接口和固定摘要 Docker 命令适配器；现有默认文本路径不变，headless 尚未装配复杂解析器。
- 成功证据记录解析器身份和版本；配置解析失败不再错误标作 bounded-text。创建确认丢失时按随机自有名称尝试清理，清理不能确认则失败关闭。真实 daemon 超时竞态仍须镜像实测。
- Python DOCX/XLSX/PDF 草稿与夹具保留在工作区，但依赖版本查询两次授权审查超时，无已构建镜像、无 Python 运行证明，不提交为已验证实现。
- 现有 Spec 投影尚忽略部分读取标志，并截断正文事实；修好不可绕过的完整性门禁及正文到模型的来源覆盖前，不启用此能力。

## D-024 — 完整回归必须保持子进程测试隔离

- HTTP 端到端测试虽使用真实服务进程，仍需显式传入测试标识，避免探测开发机模型服务和登录 shell。修复测试边界，不改生产发现功能，也不靠扩大超时掩盖依赖。
- 回归失败必须保留并对照：本次先证实 Chief 超时在未修改基线中存在，再补测试标识使原失败文件 88 项全部通过；随后正式项目测试链全部成功，才保存本地提交。默认测试链成功仍不能替代真实群聊与 Docker 专项验收。

## D-023 — 回答人逐问题绑定来源，不从 @ 列表猜负责人

- 目标问题默认找事项最初提出人，专业问题需模型给出同事项作者、事件与逐字引文；未知则为 null，保留角色提示。引文校验只能证明来源一致，不能证明模型理解职责正确，仍须真实样本验证。
- 来源和角色放入不可变 Spec 的问题记录；发消息时再次核对原消息归属及唯一 staff 身份。不同企业的相同 staff 字符串仍视为身份歧义，不自动提醒。
- 卡片保留逐问题回答人，钉钉输出显示“@某同事，具体问题”，不向群里暴露 principalId 或引文元数据。汇总通知名单不增加普通成员控制能力。
- 无凭据、Owner、生产或默认分支操作；真实模型和群内通知效果仍待授权配置与实际试点验证。

## D-022 — 归并成功不等于需求已进入执行

- 为归并后的需求投影单独记录最多三次尝试和租约，避免模型已成功但后续整理失败时无限重试。跨重启保留计数，不再次创建事项或贡献。
- 通知从持久化失败状态恢复，按来源事件去重；异常正文不入通知。已有对应解释任务或事项已结束时，不生成误导性的投影失败通知。
- 本批仅本地实现和故障注入回归；试点未配置自然会话模型，不能通过已有执行凭据隐式启用新的模型服务。

## D-021 — 自然事项归并先于需求解释，模型调用必须无工具

- 状态：可配置实现已完成，真实模型及 Docker 验收待进行。
- 自然归并读取同群事项摘要和有限近期消息；模型可建议新建、关联或澄清，不得选择群外事项或执行 Owner 操作。并发版本变化使旧建议失效。
- 真实模型适配走显式配置的 Responses HTTP 接口，不自动读取其他 API Key 或转用 Codex OAuth，不暴露执行工具，不跟随重定向、不要求服务端存储响应。
- 配置入口：`OMB_NATURAL_INTAKE_ENABLED=1`、`OMB_NATURAL_INTAKE_ENDPOINT`（完整 Responses 地址）、`OMB_NATURAL_INTAKE_MODEL`、`OMB_NATURAL_INTAKE_CREDENTIAL_FILE`（已有 0600 纯文本凭据的绝对路径）；仅明确允许的本机测试代理可设置 `OMB_NATURAL_INTAKE_ALLOW_LOOPBACK_HTTP=1`。这些配置不是让普通群成员填写的模板，也没有在本批实际设置。
- 模型/地址/凭据来自控制面，不来自聊天、附件或模型建议。缺失配置不得自动申请新付费、导出 OAuth 或改变身份。

## D-020 — 自然理解是有来源的建议，不是控制权限

- 状态：解释队列、校验和运行时后台入口已实现；真实模型装配和效果验收尚未完成。
- 模型只接收脱敏、受限的 Spec 和消息上下文，无代码执行或 Owner 动作接口；只允许目标、验收、回答和最多三个关键问题字段。
- 建议必须引用当前消息原文，绑定 Spec 版本和租约认领。短确认只能确认正在追问的现有目标；不能解决系统附件/上下文完整性门禁。
- 当前建议与结果版本原子提交；应用记录不可改写，重复消息不再解释。模型延迟不阻塞入站或续租；过期模型结果不得覆盖新消息。
- 结构引用校验不等于语义正确性证明。真实模型质量、无编号自然关联、角色身份定向和需求到测试断言的覆盖仍须独立验证。

## D-019 — 2026-09-05 新目标与证据分级

- 状态：目标已确认，分阶段实施中。
- 决策：自然群聊是核心验收项；不能以标签解析替代语义协作、以重复预设测试替代断言级业务验收、以 configured/healthy 探针替代真实消息闭环。
- 约束：唯一 Owner 权限、不可信输入边界和非生产范围不变；用户新要求优先于历史“继续任务必须填写 WI”的产品规则。
- 连接修复：仅运行时维护循环负责重连；禁用 SDK 自动重连，等待 REGISTERED，30 秒注册宽限和 1–60 秒失败退避。暂态连接故障只由实时健康计算，不永久锁住收发消息。
- 未覆盖：活连接健康探针、重启队列、自然语义解释、复杂文档和断言级验收均需要后续批次，不从本批结果推断完成。

## D-001 — 优先减少返工，而不是扩展 Bot 数量

- 状态：已确认
- 决策：三个月内以需求精确度、测试完整度和结果可信度为核心目标。
- 理由：当前返工主要来自需求不清和测试遗漏。

## D-002 — 多 Agent 用于上下文隔离，不以省 Token 为目标

- 状态：已确认
- 决策：Meta 保留控制上下文，阅读、实现和独立复测使用边界明确的 Agent 上下文。
- 理由：主 Agent 上下文越小，越容易精确维护目标和决策。

## D-003 — 文档和表格属于需求输入，不具有控制权

- 状态：已确认
- 决策：提取附件事实、缺陷和验收信息，但附件内容不能直接授予权限、执行命令或改变系统规则。
- 理由：需要同时支持文档驱动工作和提示注入防护。

## D-004 — 采用三级验证

- 状态：已确认
- 决策：Execution 自测、Verifier 独立复测、Meta 对照 Spec 验收。
- 理由：单一执行上下文容易遗漏测试或高估自身结果。

## D-005 — 简单任务不委派

- 状态：已确认
- 决策：强耦合、三步以内或委派成本高于执行成本的任务由 Meta 直接处理。
- 理由：多 Agent 是精准度工具，不是形式要求。

## D-006 — 完成状态继续按风险分流

- 状态：已确认
- 决策：完整验证的低风险任务自动完成；高风险、不确定或不可逆事项由唯一 Owner 审批。
- 理由：普通修改不应增加人工确认负担，同时不能弱化高风险控制。

## D-007 — 数据库是产品运行状态的权威源

- 状态：已确认
- 决策：根目录四个 Markdown 文件用于本轮 Meta 开发恢复；运行中的每个 Work Item 继续以不可变 snapshot、plan、run、evidence 和 audit 为权威状态，必要时从数据库生成只读上下文文件。
- 理由：多个运行任务直接写共享 Markdown 会产生并发冲突和状态漂移。

## D-008 — 先修文字 Spec，再接真实附件下载

- 状态：已确认
- 决策：第一批先解决首条消息自动确认、任务级验收条件和执行上下文缺失；同时定义文档/表格安全来源接口。真实附件下载和解析作为下一批接入。
- 理由：附件元数据不能证明正文已读取，下载凭据也不能进入 Ledger、日志或 Agent 上下文。

## D-009 — Meta 验收必须是确定性门禁

- 状态：已确认
- 决策：Verifier 可以分析验收覆盖，但最终完成条件由服务端绑定 Spec 版本、候选结果和证据确定性判断；Owner 不能绕过质量门禁。
- 理由：模型自报“完成”不能替代可复验的证据。

## D-010 — 复核结果必须成对且以最新记录为准

- 状态：已实现
- 决策：只有同一候选、同一 Spec 和同一验证契约下最新的 Verifier 与 Meta 均通过，任务才能完成或进入高风险审批。
- 理由：孤立、历史或已被后续失败覆盖的通过记录不能代表当前结果。

## D-011 — 同仓库写入自动串行

- 状态：已实现
- 决策：不同仓库可以并发；同一仓库的写入任务进入队列，前一个无论成功或失败都必须释放队列。
- 理由：保留团队并发沟通能力，同时避免多个写入 Agent 对共享文件产生竞争和不可解释结果。

## D-012 — Work Item 状态文件是可再生投影

- 状态：已实现
- 决策：每个任务从 SQLite 生成不可变版本的 `SPEC.md`、`PROGRESS.md`、`DECISIONS.md` 和 `VERIFY.md`；投影失败不回滚已提交的业务状态，连续失败三次后停止重复尝试。
- 理由：主 Agent 可以只读取当前任务的小上下文恢复工作，同时保持数据库为唯一权威源。

## D-013 — 验收证据严格映射

- 状态：已实现
- 决策：模糊的“测试通过”不能自动覆盖任意业务验收；无法映射到可信测试的条件必须明确报告证据不足。
- 理由：宁可停在未完成，也不能以通用测试冒充具体业务结果，从而制造返工。

## D-014 — 用户需求进入状态包和模型前统一脱敏

- 状态：已实现
- 决策：目标、说明、输入证据和完成条件在写入 Meta 状态包或发送给执行模型前，统一隐藏常见密钥、令牌、密码、JWT、授权头和 URL 查询凭据。
- 理由：附件和群消息属于不可信需求材料，不能假设其中永远没有误贴的敏感信息。

## D-015 — 自然关联使用确定性高置信规则

- 状态：已实现
- 决策：显式 WI 和有效回复链优先；明确换题创建新事项；只有清晰补充或标题指向唯一时才自动归并，其余展示最多 3 个业务标题澄清。
- 理由：自然协作不能要求普通用户掌握内部编号，同时不能用语义猜测把需求挂错任务。

## D-016 — 附件下载能力与业务证据彻底分离

- 状态：已实现
- 决策：Ledger 只保存公开元数据和不可逆引用；下载码与机器人码进入本地 AES-GCM Vault，完成或永久失败后销毁。
- 理由：附件下载能力可重放，不能进入数据库正文、日志、Spec 或模型上下文。

## D-017 — 先支持可安全验证的直接附件格式

- 状态：已实现
- 决策：首期只读取 UTF-8 TXT、Markdown 和 RFC4180 CSV；未知、冲突或复杂格式明确标记未支持，不降级猜测。
- 理由：这是无需额外用户授权且能严格限制解析面的最小真实闭环；DOCX、XLSX、PDF 需后续隔离解析器。

## D-018 — 附件证据投影必须可重放

- 状态：已实现
- 决策：原文件、提取记录、分块和 Work Item 证据先不可变落库；只有需求投影成功后才标记 projected，回调失败或重启会从证据重放而不重复下载。
- 理由：不能在“下载成功、规划前崩溃”时永久丢失附件内容，也不能靠重复下载恢复。
