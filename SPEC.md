# 内部研发助手 Meta 协作规范

开发模型路由（2026-09-07）：headless新增显式OMB_CODEX_OPENCODEX_ENDPOINT，将现有CodexReadOnlyPatchProvider指向本机OpenCodex Responses；必须配置模型，推理默认medium。仅HTTP字面回环和精确/v1/responses，无URL凭据/查询/片段；错误在创建Provider目录前拒绝。CLI每次使用私有临时CODEX_HOME及--ignore-user-config，固定Provider覆盖、不继承原模型密钥环境变量；结束后清理。本机常规只读模式和已有独立UID权限模式不变，不增加绕过隔离的开关。真实合成建议通过不等于实际代码应用或群服务已切换；旧路径未配置时保持兼容。

本机执行授权更新（2026-09-07，优先于下方历史待授权状态）：Owner允许受限本机通道调用宿主OpenCodex，网络问题时直接在宿主执行诊断/模型请求。首选宿主控制面回环调用Astra/medium，Docker保持不可信候选代码与附件的隔离边界，不以无沙箱模型HTTP请求扩展为代码任意执行。生产模型适配器直连及宿主Docker取消smoke分别已验证；现有钉钉服务尚未迁移，开发Provider模型路由、共享路径、VM代次/既有containment引用和单账本租约需先验证。完整目标及六类真实验收不变。

当前真实验收入口（2026-09-06）：以 [PMO 六场景手册](docs/pilot/pmo-six-scenario-runbook.md) 核对原完整目标。旧里程碑手册与v1报告保留历史用途，其要求编号/普通结果人工接受及部分并发不适用规则不得作为当前产品验收标准。新手册不是通过报告，六类及其文档/表格/附件、未@/引用子项和跨场景隔离/恢复/权限证据须分别核验；自动证据采集和新报告契约仍待补齐，不以文档完善替代真实执行。

运行配置验收一致性（2026-09-06）：schema29保存受有效实例租约约束的不可变运行策略快照，仅保存仓库及命令/限制/断言/报告器/源码清单/模型policyId哈希。每个fence只能发布一个快照；换配置需重启。正常非probe且live的headless启动在恢复/调度前发布，禁用execution时发布空仓库，restored待审账本不发布。两阶段v3收据绑定运行策略，直接完成与Owner共用读取入口拒绝旧策略、缺失哈希及已移除仓库。未曾发布快照的独立历史数据库保留兼容读取，不声称所有独立API强制配置；读取比较owner/fence，不额外检查租约到期，仍依赖既有控制门禁。历史收据不补造。

验收读取一致性（2026-09-06）：验收收据契约升级为v3，Verifier和Meta同时保存由主程序计算的当前持久Spec身份。自动完成/Owner决策等直接读取收据的入口也需重算并匹配该身份，不能仅检查两条历史收据彼此一致；读取/禁止范围变化后，不等待下一次复测即可阻断旧结论。旧v1/v2收据不补造新身份，正常重新验证生成新证据。该身份覆盖持久Spec与计划范围；未经过协调器的独立读取入口是否绑定最新进程外命令/模型配置仍需专项审计，不能把本项称为所有运行配置漂移保护已完成。

验收源码上下文（2026-09-06）：受信任命令可用 acceptanceSourceFiles 明确指定仓库相对的业务实现文件，不由普通群消息或模型授予读取能力。测试文件和实现文件均需符合当前独立验证节点的 readScope/denyScope（包括禁止祖先目录）；全部读取对象固定于同一 candidateSha，并保留文件/blob身份。读取前检查全部路径，拒绝隐式导入遍历、越界/通配/重复、敏感目录、符号链接、缺失和超限。实现来源 role=implementation 只能辅助核对，不得绑定成测试文件；源码清单及读取/禁止范围纳入验收契约，变更使旧结果失效。源码清单不证明依赖完整，缺失关键调用链仍返回未完成。本批是安全读取能力，不等于真实群/模型/Docker交付验收。

源码脱敏视图（2026-09-06）：固定候选的测试源码采用有界 TypeScript 语法解析，不执行代码、不解析依赖、不创建类型检查程序。识别凭据字段/赋值/比较/默认值/设置调用中的字面量、常见敏感文本与注释；保留引用、运算符、非敏感证据行及换行，重复处理必须不变。未知动态字段保守脱敏；解析失败、超限或处理异常以静态错误停止，不输出诊断原文。源码采集与送模型前复验使用同一处理器，普通需求文字仍用文本脱敏。脱敏视图不作为可执行代码、不保证语义等价，也不是任意编码或混淆 Secret 的通用检测器；必要值隐藏或被测依赖缺失仍须拒绝覆盖结论。当前仅采集显式测试文件，不能据此宣称完整业务已覆盖。

验收映射安全说明（2026-09-06）：两个模型角色的说明只描述输入类别、断言关系和业务结果，不复述凭据样式的具体测试值；不还原已脱敏内容。现有敏感输出拒绝及收据不落敏感原文的规则保留。必要源码被脱敏或依赖缺失时必须保持不确定/缺失，不能从测试名称、局部函数或固定文本断言推导真实业务流程已覆盖。

验收映射条件身份（2026-09-06）：模型只选择主程序提供的 conditionHash，不计算或猜测密码学标识。Proposer 与独立 Verifier 均收到由同一脱敏后 canonical request 派生的条件及标识；canonical request、requestHash、收据和候选绑定不变。模型视图不接受用户自带标识，也不赋予执行或审批能力。正确标识不是语义覆盖或测试通过证明。

可回答问题契约（2026-09-06）：模型输出 schema 的 answers.questionId 仅允许当前 questions 中的 natural- 业务问题，排除 natural-input-pending 与 natural-context-incomplete；无符合项时 answers 必须为空。生成约束和运行时校验共享同一判定，模型仍不能回答过期/不存在问题或清除系统状态。系统门禁保留在输入上下文，目标/验收分别通过对应字段补充，不伪装成 answers。新增问题编号由系统加前缀，模型不重复添加。

自然目标确认的模型契约（2026-09-06）：明确需求无需技术模板或为润色额外确认。模型输出 confirmed=true 时保留当前消息内连续逐字的目标；仅当前存在目标确认问题、用户明确回答且目标与当前 snapshot.goal 完全一致时允许上下文确认。quote 的原文存在不授权确认改写或新增范围；需要归纳但不满足条件、或目标超过长度限制时保持未确认并澄清，不截断后当成完整确认。本批对齐模型指令，不放宽运行时原文/来源/版本、权限或就绪门禁。

OpenCodex 本机接入（2026-09-06）：自然解释和验收映射的每个角色可显式选择 `TRANSPORT=opencodex_local`；必须指定模型和端点，推理默认 medium，按 Owner 当前选择配置 gpt-6-astra/medium，不自动换模型或降级。不读取凭据文件、不从其他环境变量借用密钥、不发送 Authorization；只能连接显式 HTTP IPv4/IPv6 回环地址的 `/v1/responses`，拒绝远端、用户信息、查询参数、片段、重定向或混入凭据配置。原默认 HTTPS/凭据模式保持独立。所有请求无工具、无会话复用、store=false；使用 JSON Schema 输出约束与流式协议。流总量上限 256KiB、输入上限 128KiB、总期限 60 秒，取消后丢弃迟到结果并清理响应。只有匹配的消息/文本/结束事件和同一响应 ID 的 completed、精确模型/推理强度一致才返回解析后的 JSON；错误、截断、拒绝、工具事件和冲突均不能生成成功结论。完成事件可为空 output，但非空时必须与流内容一致。Proposer/Verifier 分别构造无历史上下文，策略身份包含传输/模型/端点/推理强度，旧证据不跨配置复用。本机最小模型请求不等于真实需求理解、Docker 接通或六类场景通过；Docker 内回环不视作宿主回环，不为方便而拓宽无认证远端。

创建回执丢失的文档资源恢复（2026-09-06，优先于下方未知 ID 不恢复的历史边界）：同一显式 Docker context、已知旧实例 fence 的未清理记录即使没有 ID，也参与原有三次持久尝试预算。只 inspect 创建前已保存的完整随机名称，不扫描名称前缀。必须得到唯一容器、完整 ID、精确名称、资源标签和原固定镜像证明；在当前有效实例租约下，以未变化的资源快照和最新尝试次数为条件原子保存发现的 ID，之后才按该 ID 清理并独立确认缺失。保存失败、迟到的冲突创建回执、新恢复尝试、取消或实例接管均不能继续删除。未知名称查询失败或不存在不等于无遗留，保持未解决，三次后停止自动尝试。无归属旧记录、当前实例和其他 context 仍排除。受控 Docker 端口和真实 SQLite 验证不等于真实 Docker 强杀、独立 supervisor、主机重启或六类群聊验收；本批不启用解析器、不操作实际容器，schema 保持 28。

异步等待后的实时租约检查（2026-09-06）：Stream维护完成后，使用当前运行时钟重新检查实例租约，再开始发送/核查；发送后及后台维护返回后再次检查，过期即退为lease_failed，停止本轮后续工作，不把等待前的时间当作仍有权限的依据。普通Outbox发送落账也按包含传输耗时的完成时间检查实例与认领期限；迟到成功/失败结果不能更新原认领，原消息保持不确定而非授权重发。此修复不延长租约、不自动接管、不改变Owner身份或审批；不证明主机重启或真实平台验收。

待核查回复自动查询（2026-09-06，schema 28，优先于下方未接入调度的历史边界）：运行时原 Outbox 调度会交替处理正常发送和只查询核查；无正常发送时也处理待核查记录，不重新调用发送方法。每次查询前追加独立不可删除的尝试记录，绑定完整 Outbox 快照摘要、实例 owner/fence、认领时限和下次时间，每条最多三次，重建和查询中断不重置；无回执直接停止、未知状态退避后再查、成功结果与 Outbox sent/序列/归属选择记录同事务保存。原发送次数不变化；不确定、耗尽记录仍为待核查，不伪称未发送。完成时按本次查询实际耗时检查租约和认领，要求仍为最新尝试及同一未被替代的 Outbox 快照；实例替换、原回复状态/内容变更或通知已过时不得应用旧结果。新表不保存回执、访问令牌、上游错误原文或已读人员。旧库升级不虚构历史查询。当前不提供三次耗尽后的 Owner 恢复/补发，未建立远端回复 ID 映射；无回执的崩溃窗口和真实环境六类验收仍待解决。该自动核查只确认消息发送状态，不代表代码任务验收。

主动群消息持久回执（2026-09-06，优先于下方单次查询边界）：普通主动发送的有效受理回执在查询前保存到独立私有目录，以既有应用 Secret 经独立用途 HKDF 派生密钥，AES-256-GCM 加密；不新增凭据、不写业务 Ledger/模型/日志。不可变绑定 Outbox ID、去重键、应用、群、原 payload 和实际渲染摘要；以完整临时文件 fsync 后无覆盖发布并同步目录。收到成功回执但存储失败仍为 unknown，不查询或重发；发送到保存之间的崩溃仍由 Outbox 原有不确定规则停止，不能声称已消除该窗口。真实运输提供 reconcile 查询入口，重建后只查原消息；已有回执时不能改走 session，路由/内容/应用变更、解密或权限错误均停止。缺回执仅表示缺证据，不授权重发。本批不自动调度 dead_letter 核查、不修改队列成功状态、不提供补发/回执删除或远端引用映射；后续须接入有持久次数、退避、fence/时效与 Owner 决策防竞争的后台核查。Secret 少于 32 字节的主动回执存储拒绝；不自动更改实际 Secret。

主动群消息发送确认（2026-09-06）：普通 Markdown 群消息获得 processQueryKey 后，使用原发送 app/群和原查询标识调用官方群消息状态查询；仅 sendStatus=SUCCESS 且业务字段无矛盾才记录发送成功。PROCESSING、RECALLED、缺字段、错误/超时均表示当前无法确认送达，进入既有待核查队列，不自动重发。查询最多四秒、响应最多 16KiB、仅取一位已读用户的第一页但不保留人员数据；与原 session/token/send 三个八秒预算合计最多 28 秒。查询标识不作引用消息 ID，不进错误/日志/模型。本批不持久化查询标识、不后台重复查询、不证明完整送达对账；互动卡片和 session 仍沿原契约，真实群/权限/六场景未验收。

真实入口契约审计（2026-09-06，优先于下方实现能力表述）：官方消息类型文档明确群内 @应用机器人不支持接收文件；普通群聊非 @ 投递、引用消息字段和出站映射尚未验证。Webhook 支持 @ 不等于备用群消息 API 支持 @。内部附件/自然关联的合成测试不证明这些真实入口成立，产品目标保持不变，不能缩减成只支持 @文本或文档链接后称完成。详见 packaging/collaboration/dingtalk-capability-audit.md；新增身份/授权通道须 Owner 明确批准，禁止复用其他登录身份。优先补真实通道证据，再扩展边缘交互。

附件选择定向澄清（2026-09-06）：单份可读新材料已经完整核验、同条原消息有多份同提供者的未读材料且尚未指定目标时，明确询问该文件对应哪份，最多展示三个原序号/脱敏文件名及超出总数。问题通过持久来源中的提供者及已核实 staff 别名定向提醒，不默认提醒事项创建者；有歧义身份不猜测。提示回复上传新材料的那条消息说第几份，不要求内部编号。等待选择时暂不展示重复上传问题，但未读门禁继续存在；选择后恢复剩余材料核对。新材料部分可读、他人材料、已有成功正文、冲突或越界选择不冒充可用替代。本批不扩张选择语法/权限，不证明机器人回复引用链或真实群聊已验收。

多附件精确替代（2026-09-06，优先于下方单附件历史边界）：同一原消息包含多份未读附件时，原提供者可以在上传单份完整可读替代材料时，指定原上传序号或唯一精确文件名；也可随后回复这条替代上传消息，说明“第二份”“替换第2份附件”或“是other.pdf”。选择必须来自同群同事项同提供者的已保存后续消息，无新附件，原序号不会按剩余文件重新排序。选择来源的消息 ID/规范化哈希和原始序号随替代收据进入模型上下文、指纹和当前 Spec 应用证据。重复名称、越界选择、冲突选择或多个有效替代不猜测；未选中的未读材料继续阻塞。回复明确对应原消息第几份、原文件保留和尚未开始修改，不把材料确认当开发完成。本批只覆盖有界确定性表达；多份新材料同时替代、冲突后的改选、已使用事实变更及任意自然语言仍未完成，真实模型/群聊/文档隔离验收独立进行。

可读附件替代（2026-09-06）：在同群同事项中，原材料提供者回复包含单份旧附件的原消息，随单份新附件明确表达“用这份文件替换原附件”等替代意图时，系统从已保存的消息来源推导关系；仅旧附件 failed/unsupported 且无成功提取、新附件正文来源可核查且完整时，旧附件不再构成当前输入完整性阻塞。原文件、错误和双方消息不删除或改写；双方附件/消息 ID、提供者、规范化消息哈希及新内容哈希进入自然解释上下文和应用收据，并绑定附件上下文指纹。否定/引用内容不触发；仅同名或附件正文指令不触发；他人材料、多附件、多个有效替代或已有成功正文不得自动覆盖。新来源漂移重新阻塞，原失败提示在发送前被抑制。此功能只处理未读材料的明确替代，不授予重试/审批/删除权限，不确认业务目标或宣称代码已修改。当前为有界确定性表达/原消息引用路径；更广泛自由表达、多附件精确选择、多个有效替代的后续选择和已用材料变更仍待后续实现及真实群验收。

附件需求整理中断恢复（2026-09-06）：对已经提取正文但整理认领过期、结果尚未确认的附件，重新领取前必须将旧 token 的未确认结果、退避时间和必要业务通知同事务落库；不能静默替换旧 token 而抹掉一次尝试。连续三次通用不可用结果（含中断）沿现有规则停止，由唯一 Owner 通过“继续整理附件”恢复新窗口。正常未过期认领不触碰；新窗口不会删除旧失败，重放恢复请求不重复授权。不重新下载已提取正文、不假称整理或代码修改完成；旧 token 迟到结果拒绝。该证据为 SQLite/协调器重建与受控回调，不等同真实进程强杀或钉钉试点。

文档资源恢复（2026-09-06，schema 27，优先于下方历史边界）：新资源身份绑定运行实例 owner/fence；已失去租约的旧 fence、同一显式 Docker context、拥有完整已知容器 ID 且未确认清理的记录才进入自动恢复。每次 Docker I/O 前先持久化尝试次数（最多三次，重启不重置）；inspect 必须核对完整 ID、名称、资源标签及固定镜像，才能按 ID 删除。随后独立查询该 ID，只有成功查询且为空才记录 recovery_verified_absent。无法 inspect 时仅允许独立查缺，不盲删。取消、租约变更或实时认领过期立即停止并向调用方传播；单条 Docker 失败保留证据。恢复在启用解析器的后台摄取批次开始时执行，不阻塞 Stream 接收。旧版本无 owner/fence、未知 ID、当前实例和其他 context 均不自动清理；迟到的已知 ID 入账后才能参与后续恢复。实例归属、已知 ID 与恢复证据不可倒退。本机制不是独立 supervisor，不证明创建回执永久丢失、真实强杀或完整主机重启下无遗留；三次耗尽后的 Owner 恢复入口尚未实现。

文档资源归属账本（2026-09-06，schema 26）：headless 启用解析器时，创建前持久化唯一容器名称、固定镜像、Docker context、源文件哈希及时间；创建回执中的容器 ID 持久保存后才允许 start。容器带与账本名称一致的资源标签。rm 成功只记录 cleanup_acknowledged，不等同于独立确认无遗留；写账本失败不继续下一副作用或交付成功。标识不可修改、已知 ID 不可替换、清理回执不可倒退、记录不可删除。待核查读取限定配置 context，最多 100 条；没有自动清理、过期判定或完成总数含义。历史版本升级不生成虚构资源归属。强杀后的回收须再绑定失效实例、核对实际容器身份并验证创建迟到，不能仅按账本或名称前缀删除。

文档解析生命周期（2026-09-06）：协调器将受信任 signal/认领检查传入解析器；停止前不得新建解析容器，创建返回后失去认领不得启动，解析/清理期间停止不得交付正文。创建请求等待既有有界超时/回执后清理，不因普通 abort 主动丢掉创建回执；start 命令支持 abort，等待 CLI close 后再移除本次容器，清理不继承已取消的信号，清理失败保持独立错误。本批仅覆盖存活服务的停止链路，不承诺 SIGKILL、创建迟到、Docker daemon 失联后的持久回收或主机重启恢复；这些仍须真实隔离与独立恢复机制证明。

文档解析后台装配（2026-09-06）：headless 将受信任配置的解析器注入附件协调器，默认关闭；明确启用必须同时指定固定镜像和 Docker context，配置错误在创建运行时前拒绝。仅配置镜像不启用，健康检查不启动解析或下载。来源哈希、解析器版本、页/单元格位置、不可信标记及不完整提示沿既有账本/投影保存；解析失败不伪报已读取，服务停止后的迟到结果不得提交。真实镜像隔离 smoke 完成前不启用现有试点，在线文档授权及六类真实群聊仍独立验收；本批不证明即时取消或强杀后容器清理。

普通直接控制与回复原子化（2026-09-06）：headless 对暂停/恢复/重试/取消/批准/退回启用受信任 replyRequested 选项，在原控制器事务中提交控制变更、审计、不可变来源收据与业务回复；入队失败整体回滚。既有核心调用默认不新增消息；旧收据重放仍保留原记录，不补造来源，运行时原兼容回复入口可处理历史缺回复但不证明已自动回补。入站消息不能复用已处理控制事件，控制也不能复用已入账消息/恢复/回复事件。仅将原文案提取为共享渲染，不变更审批权限、三次停止规则或正常用户协议。

Token 审批事件与回复原子记录（2026-09-06）：headless 的卡片/兼容文本审批将事件身份传入原控制器，在消费 token、控制变更和审计的同一事务保存 kind=token_action 不可变收据；文本审批同时保存待发送回复。任一步写失败整体回滚，同一事件重放只返回原始结果，不重复控制/审计/回复；修改 token、人员、来源群、原因或入口类型时拒绝。仅保存请求摘要，不持久化可执行 token。规范化文本消息可保留群来源；卡片回调没有已验证群来源时只保存结果，不猜群或新增消息。旧独立桥接器和历史无收据回复不因此被视为已迁移，卡片真实回执/送达与群内验收仍待完成。

状态查询/刷新审批的原始结果（2026-09-06）：两类新请求在现有不可变文本命令收据中记录 kind=owner_query、请求群、请求摘要与原始处理结果，并与回复/被替代消息状态在同一事务提交。重放只返回原始结果，不因已有回复把拒绝改成成功，不重复刷新；改变群、人员、事项或命令的重放拒绝。旧回复没有收据时保留不确定，不补造来源或重发。查询事件不能再被当新需求或 token 动作处理。状态查询仍可由普通成员使用，刷新审批仍仅唯一 Owner；这是现有 WI 协议可靠性修复，不等于所有自然指代/自由措辞已支持。

直接文本控制回复来源（2026-09-06）：暂停/恢复/重试/取消及批准/退回命令将规范化入站的 conversationId 传入控制器，与控制决定、审计及不可变请求收据同事务保存。新收据的来源不能在重放中更换或删除；旧收据不补造来源。发送端识别控制收据后使用原请求路由，不按目标事项最近消息替换来源，旧来源缺失时只能使用仍可用的原 session。此批不改变权限、控制动作或普通用户交互格式；状态查询、刷新审批、token 文本/卡片动作及动作到回复的跨事务崩溃恢复仍需补齐，不能把局部来源修复称为完整控制闭环。

自然恢复回复来源（2026-09-06）：新的“继续整理需求/附件”恢复请求，在现有不可变收据中保存原始 conversationId，与恢复结果、审计和回复同事务提交；拒绝或需要澄清的回复同样保留来源，不要求已有 Work Item 才能正确回复。备用路由读取该来源并使用已配置群映射，空 session 的发送器重建后仍可发回原群，成功入账的回复不因重启重发。来源字段在旧收据中可缺省，重放旧请求不能补造来源或新增发送授权。权限、恢复次数边界、Spec 和群白名单规则不变；普通文本控制/卡片动作来源及真实群验收另行完成。

多群备用投递（2026-09-06）：主动发送必须从持久化原事件（或 Owner 核查查询收据）确认来源群，再查受信任的 `OMB_DINGTALK_PROACTIVE_CONVERSATION_MAP` JSON 对象，键为白名单 conversationId，值为对应 openConversationId；两种 ID 不推定相同。未知来源不从正文、任务标题或全局默认群猜测。旧 `OMB_DINGTALK_PROACTIVE_OPEN_CONVERSATION_ID` 仅在单群白名单且来源匹配时兼容；多群无明确映射时禁用备用路由，原 session 回复仍可用。映射与旧配置冲突、目的群重复或源不在白名单时拒绝配置。尚无持久化来源群的旧控制回复只能使用原 session，不冒险跨群补发。新增配置接口不代表已修改实际配置或完成真实群验证。

## 目标

在三个月内，让个人和内部团队能够在钉钉群中并发提交研发任务，并稳定获得精确、简练、可验证的开发结果。系统优先降低因需求不清和测试遗漏造成的返工。

## 本轮范围

Owner 自然查询待核查回复（2026-09-06）：直接对机器人说“查看待核查回复”（兼容未确认/未送达的回复）走受控只读业务查询，不创建研发事项。只允许当前唯一 Owner，范围限定当前白名单群；引用原需求时只查该来源对应的同群事项，未知/跨群引用拒绝而不猜测。最多展示 5 条业务标题和回复类型，完整总数与固定投递指纹保存在不可变查询收据。查询和新答复同事务持久化后 ACK；不改旧投递、不重发、不改变 Spec/任务。Owner 代次或原投递记录变更后不发送旧清单。此批不提供送达确认/补发，未完成机器人出站引用或真实群验收。

投递排查可见性（2026-09-06）：后台健康状态独立输出 delivery 状态、通俗说明及 queued/sending/retrying/needsReview 数量；只聚合当前未送达且未被替代的回复。停止消息、过期认领、无明确未发送分类的历史重试均列待核查，不把它们断言为已失败或未发送。数据库不可读/服务未打开时返回 unavailable 与空计数（null），不能冒充零条。读取不写队列、不重发、不产生 Owner 授权；单条回复待核查不改变服务 readiness，不封锁正常任务。此处是本机后台/健康检查可见性，不是群内主动通知、消息级核查或安全补发功能。

回复业务状态一致性（2026-09-06）：成功/错误字段必须类型正确且互不冲突；显式拒绝与成功标记或有效普通消息查询回执并存时，保留送达未确认，禁止自动换通道或重发。数字错误码不得忽略，令牌响应带拒绝或异常字段时不缓存、不用于发消息。普通主动消息仍须有效 processQueryKey，通用 success/code 不能代替该回执；此查询码不等于远端消息 ID。卡片具体送达回执及机器人引用映射仍需独立接口证据，不以通用一致性检查宣称卡片送达验证完成。

回复请求有界等待（2026-09-06）：session、主动消息/卡片及访问令牌交换每次请求共享 8 秒响应头与正文期限；session 回退加令牌交换与主动发送最多三个网络期限，低于默认 30 秒 Outbox 认领期。成功 JSON 按实际流字节限制为 session 16 KiB、主动消息/令牌 64 KiB，不先无限读取后才检查。超时主动中止请求/取消正文，即使测试 transport 忽略中止或取消不结束也释放调用方；迟到响应丢弃。取令牌失败仍为未提交消息；提交后超时/无法完整读取为送达未确认，不换通道或自动重发。此批不证明钉钉远端实际送达，也不补齐人工核查协议。

钉钉投递不确定性（2026-09-06）：Bot/Webhook 没有已证明的远端幂等键保障。生产投递在丢失回执、HTTP 408/5xx 或无法验证业务结果时，保持“送达未确认”，不能换通道或自动重发。过期发送认领和旧版无明确未发送记录的待重试消息进入待核查队列（内部 dead_letter + unconfirmed 原因），sent_at 保持空，不算已送达或确认失败。明确在发送前失败或收到业务拒绝时仍可有界重试/回退；安全重试分类由主控持久化。此为防止重复发送的保护，不是端到端 exactly-once 或最终送达证明；人工核查/安全再发送协议、远端回执映射仍待补齐。

引用上下文与乱序续办（2026-09-06）：引用未找到、跨群或尚无归属时，不得忽略引用再用关键词、模型高置信度或“第二个”猜测归属，也不因此新建任务。原消息和引用持久保存，并给出通俗的缺上下文提示。已启用自然整理时，原消息迟到或后来确认归属后可自动恢复引用补充；只跟随已入账、同群且尚未结束的事项，事务复核引用关系并保存来源，不重新要求用户发一次。机器人出站消息回执尚未实现，processQueryKey 不能未经证据当作 originalMsgId，不能把本批验证宣称为机器人引用全支持。

需求整理恢复（2026-09-06）：连续三次解释失败后，当前唯一 Owner 可直接说“继续整理需求”或“重新整理需求”。同群只有一个符合条件的事项时恢复该事项全部已知失败输入；多个事项必须回复原需求或补充消息定位，不猜测。只重开需求解释窗口，不执行代码、不改变当前 Spec、原消息或 Owner 身份；不可变记录绑定原失败、源摘要和 Owner 代次。旧授权重放不能开启后来失败窗口，新窗口仍三次停止。失败通知绑定恢复代次和当前需求版本，恢复后旧通知不再发送。该受控恢复短句不是所有自然表达的理解证明，真实群仍待验收。

实际 Linux 恢复验证（2026-09-06）：缓存固定 Node 镜像中运行当前生产 service/SQLite，以合成多人消息持久入队；确认 durable 后强杀原服务进程，新进程恢复全部补充，重放不产生新事件。实际检查无网络/非 root/只读根、资源限制、正常退出和仅清理本次容器。解释器仍为受控端口，证明服务进程崩溃后的队列/需求记录恢复，不证明真实模型、钉钉投递、完整主机重启或六类真实试点。当前业务版本完整 pnpm test 已重新通过。

长会话增量整理（2026-09-06）：新收到的每条补充均保留独立需求解释任务，不因下一条消息到达就视为已处理。同一事项的解释串行；未处理/失败输入保留门禁，不能提前进入研发执行。模型读取当前完整消息、相关引用/待确认人的来源和最多 12 条、24,000 字符的有界历史；已应用且来源摘要匹配的旧输入可由持久 Spec 承接，不因总消息数超过 12 永久阻塞。旧版没有覆盖收据、失败或已替代的历史不能伪装成已读，超出窗口仍须明确核对。该机制不证明真实模型理解准确率，也不补造机器人消息回执。

附件整理恢复（2026-09-06）：负责人可发送“继续整理附件”或“请重新整理附件”，不要求任务编号。只恢复同一钉钉群中已连续三次停止且尚未完成的附件投影；多候选先澄清，回复原附件消息并指定第几份可缩小范围。稳定身份校验、不可变恢复记录和事件内容摘要绑定本次失败边界；不删除历史证据、不重新下载、不直接重跑代码，不声明根因已修复。该入口只处理明确短句，不将引用或附件内文字视为控制命令。真实群展示和更自由措辞仍待试点验证。

2026-09-05 新 Goal：PMO 式自然协作入口 + 可验证研发交付引擎。以下是目标范围，不是已完成声明。

附件响应性（2026-09-06 已实现，受控回归通过）：入站路径仅校验已注册来源并持久化私有下载能力，后台维护单批下载/提取/投影，不等待网络后才确认消息。下载总时限默认 15 秒，覆盖取 token、解析地址和所有流读取；服务停止/失租取消等待并校验当前租约、附件尝试。挂起处理不阻塞续租、其他消息或 Outbox。退出有界等待，未收束不释放租约、不允许同实例重新启动；不能把取消等待当作解析器进程已终止。下载和需求投影的连续三次同类失败停止/简练反馈已实现；投影停止后的 Owner 恢复已补齐本地验证，下载停止恢复、真实 Stream 下载试点及复杂解析器进程级生命周期仍待补齐。

- 群内自然新建、多轮补充、引用机器人回复和多人协作；不要求 WI 或固定模板。只追问 1–3 个真正影响结果的问题，记住答案并定向提醒。
- 自然材料生成带来源的结构化 Spec；模型只提议需求变更，不授予权限、不决定执行命令或 Owner 身份。
- 安全读取文档、表格和附件正文（含 XLSX、DOCX、PDF）；在线文档使用明确授权的读取能力。截断和未读部分必须显式阻塞相应验收。
- 每项完成结论关联当前 Spec、固定候选和实际断言证据；不以命令名命中、计划或“已记录”冒充业务完成。
- 单一重连调度、注册完成的实时连接状态、持久化队列重建、同仓库串行及不同仓库并发。
- Docker 非生产六类真实验收：自然新任务、多人补充、文档/表格 Bug、需求澄清、低风险自动完成、高风险唯一 Owner 决策。六类未全部通过前，目标保持进行中。

### 已有实现基础（不代表完整自然协作已实现）

需求投影失败补充（schema 23）：每次投影采用新的认领标识，成功/失败提交事务中重新检查活动实例和当前认领。失败保存不可变安全分类及退避时间，连续三次同类失败后不再自动投影；重启不清空预算，不重新下载或删除已提取的正文。首次/停止反馈与失败记录一起入账，明确“文字提取记录已保留，但尚未确认需求整理结果”，不误报没读到或修改完成。已恢复的旧提示被投递门禁抑制，排队筛选先排除停止/退避事项再取批次，避免堵住其他附件。未提供绕过停止状态的自动恢复；需后续唯一 Owner 的受控恢复协议。当前本地无端口协作回归通过，不替代真实模型和全仓验证。

附件确认组合验证补充（2026-09-06）：生产 Stream adapter → runtime → 真实 Ledger/Vault/coordinator 已在同一测试中连接，SDK 和下载端口仍为受控夹具。确认前必须可重新读取保存的能力；Vault 保存失败或 SDK 确认失败后重放仍只有一个事项/附件/回复。确认后立即服务重启，无需再次发送即可开始读取；慢下载期间租约和回复队列继续，停止后的迟到结果不写失败收据或证据。该证明只覆盖本地服务重建/入站边界，不替代真实 SDK 网络、完整主机重启和正文到 Spec 的成功投影。

附件失败反馈补充（2026-09-06）：schema 22 为下载、能力读取和文件保存失败保存不可变逐尝试收据，连续三个已确认的同类失败后停止；不同原因不累计为同类失败，升级不补造旧次数。首次失败提示正在重试，终态说明未读取及下一步；下载失败状态、收据和回复同事务入账，重放不重复通知。投递前检查当前附件状态/尝试、事项版本及控制状态，回复原附件消息通道。此项更新上文“三次停重试/通俗反馈待补齐”的下载阶段状态；证据投影回调异常、失控解析进程及完整真实试点仍未完成。

验证夹具补充：非生产假 Claude 同轮补充 E2E 采用 stdin 输入事件握手，不再要求观察和发消息在 800 ms 内完成；仍断言同轮接收、转录顺序与仅一次回复，保留原时限。此为全仓回归稳定性修正，不是钉钉自然协作/真实模型能力的实现或验收。

文档正文投影补充：新摄取的完整正文不再每个 chunk 仅取前 2,000 字，而是拆为带文件/原始行/片段位置的有界事实，完整性门禁逐段核对；少掉尾部仍阻塞。保留现有 12 原始 chunk、100 条事实及模型附件 48 KiB 的容量边界，超限不算已读全。长行原始提取和事实二次分段均不拆开 UTF-16 surrogate pair，避免表情等字符入库变乱码。受控解析适配器到 Ledger、Spec、自然解释和服务重建已有测试；不是实际 Office 文件/Docker/群投递证明。旧版本已经截断投影或保存乱码的历史附件尚未自动迁移/重提取。

文档容器验证补充：新增固定镜像/显式 context 的验证入口，自生成七份 Word/Excel/PDF 夹具并调用生产解析适配器，另测超时清理。通过条件包含实际隔离配置、进程状态/退出码、预期拒绝 JSON 和逐个容器消失证据；不能把通用异常或补偿清理当作解析/适配器通过。受控 Docker 端口报告与真实 Docker 报告分开标识。镜像构建及真实运行仍因公开入口 DNS/下载失败未完成，未接入 headless 真实附件；正文 Ledger 摄取/恢复与六类试点仍是完成条件。

恢复通知时效补充：Outbox 在每次开始投递（含重试和过期认领接管）前，核对通知所指 session、当前事项版本/计划/最新快照、控制状态及退出结算；状态变化后的旧恢复提示标为 superseded，不交给发送器。新的同仓库活动也使旧“可以继续”提示失效。通知使用任务消息关联回复通道，兼容旧 work_item 类型积压通知。该检查发生于 transport 调用之前，不保证撤回已经进入网络请求的消息；无有效 session 时仍依赖已有主动发送配置，未自动新增凭据或群路由。

后台恢复接入补充：runtime 进入 running 后对旧 execution/verification session 做一次后台恢复，最多四个仓库并发、同仓库串行，每项被动检查默认五秒超时。被动权威读取可取消，关机/失租后没有迟到结算、通知或调度；未知状态继续占用并排队中文说明，不展示 WI/内部状态。安全释放后只检查当前候选复核及真正未启动的待办，不把孤立执行预留当成新任务。退出结算后的漏发通知可从不可变收据补回。旧无生命周期记录的 legacy Run 仍走原启动恢复；已确认退出但 Run 仍标 running 的状态收束、完整主机重启及真实群聊仍需继续验证。下段“尚未接入”是上一批边界，本段为当前状态。

被动恢复补充：schema 21 在执行/复核协调完成后保存不可变收束标记，冻结命令及证明集合；新有效实例只在原实例失效、协调已收束且全部原始证明经独立核验为空时释放占用。零命令复核可凭旧实例已失效恢复，因为未来命令仍须原租约预留；零命令执行不得这样推断，工作目录准备可能尚未结束。恢复只记退出结算，不改任务/候选/复核结论，不杀进程、不自动重试。底层 API 已实现，尚未接入 runtime 后台扫描和群内通知，真实重启续办仍待验收。

执行层补充：schema 20 从工作目录 prepare 前登记持久仓库占用，Agent 和自测命令逐一留存启动绑定与独立证明，只有全部进程被确认为空且原 lease 有效才结算。修改/复核相互排斥，单事项 attempt 唯一；终态 Run 或新实例不能替代退出证据。新执行器和独立子进程的竞争已有本地验证，遗留 v19 运行的迁移隔离、受控清理及真实重启恢复尚未完成；未部署到真实试点。

底层复核补充：CandidateVerificationCoordinator 自身负责 schema 19 持久生命周期，独立调用和 headless 调用遵循相同租约、预留和结算检查。第二数据库连接及独立 Node 进程已验证重复复核被阻止；旧租约迟到结果不得写入复核/Meta 记录。该证据不涵盖下层 CandidateExecutor 的执行准备、失败进程与自测清理，它们仍需统一持久仓库互斥和恢复协议。

混合队列补充：headless 的公开直接执行入口与自动调度共用仓库占用，从工作目录准备前开始，到执行结束后释放；同事项的自动重扫不重复启动。自动执行后的独立复核仍持有原队列槽，Owner 复核重试遇到直接修改时排队。已补运行时混合队列、停止边界和 prepare 并发测试；底层 service/executor/coordinator 的统一持久预留、跨进程安全和真实重启验收仍需完成。

后台启动补充：启动扫描只建立候选复核队列，服务进入 running 后后台执行；维护循环不等待复核，继续接收消息、投递回复和续租。复核占用与修改调度共享 canonical repository 队列，同仓库串行、不同仓库可并行；已完成的失败尝试不会因维护循环而无限重试。未结算持久记录仍禁止重新执行；遗留记录的安全恢复、真实 Stream/Docker 强制重启及完整自然协作验收尚未完成。下方各节“尚未后台化”是对应历史批次的边界，并非当前实现状态。

持久复核补充：schema 19 记录复核、命令预留、隔离证明和退出结算；headless 新实例读取未结算仓库，避免重新启动同仓库执行或误报完成。结算须重验当前实例租约及每条命令的空进程证据。已覆盖双数据库连接/新 runtime 夹具，尚未实现遗留记录的受控解除、跨进程压力验证或后台启动，不能称为完整重启续跑。

Docker 取消补充：质量门禁向 runner 传取消信号，启动前/登记等待/运行中取消均进入本次容器身份核查与退出确认，未知则保留材料并阻止同实例继续。真实缓存镜像已验证启动门前未执行、运行中父子进程退出及临时容器清理；verifier 持久运行/恢复、跨实例安全与 startup 后台化仍未完成。

复核取消补充：运行时停止后取消模型等待，迟到模型不触发下阶段，迟到测试结果不写入复核记录。关机限时未收束会明确报告并保留租约，同一实例对象不得立即重启。进程内取消不是操作系统进程已终止的证据；verifier 持久运行/隔离记录、跨实例恢复及 startup 后台化仍未完成。

复核反馈补充：等待核对、复核受阻和执行失败分开展示，普通中文说明原因并隐藏候选技术详情；通知入队绑定当前实例、计划/Spec、控制状态和最新执行/复核尝试。迟到结果不覆盖暂停/取消或已更新事项。此能力不代表启动模型等待已后台化、主动进度通知、待核对自动续办或真实群内及时响应已验收。

模型装配与最终收据核查补充：headless 现支持显式开启的 proposer/verifier 模型配置，不读取其他功能的凭据，构造/健康检查不发请求。有效策略身份由两端配置及规则版本派生；最终完成判定会从不可变映射收据重建契约，核对 Spec、候选、条件以及自测/独立复测实际使用的绑定。暂停/取消、更新需求、候选提交或工作区改动发生于映射等待期间时，停止后续测试。可选 Compose 模型模板已建立但未部署，真实模型语义及群聊场景仍待验证。

自动验收对应补充：新增固定候选 Git blob 采集、受限映射提议、独立上下文复核与 schema 18 持久收据。当前支持受信任 Node 测试命令中的显式测试文件，记录候选/Spec/源码 blob/策略身份以及逐行引文，拒绝不完整、过时或不确定映射。运行时可注入后接入 CandidateVerificationCoordinator，动态契约仅限本候选、不修改全局命令配置；后续仍要求自测与独立复测断言证据。模型 HTTP 配置、真实语义效果、自动新增缺失测试、依赖源码上下文及真实试点未完成，不能据受控模型测试宣称业务覆盖已经准确。

断言级验收补充：命令成功不再通过名称/参数文本匹配覆盖业务需求。受信任测试配置必须把当前验收条件哈希映射到具体断言；开发自测与独立复测分别提交绑定本次 run/nonce 的有界结构化报告。缺失、重复、过期、失败或所需用例跳过均不能完成；其他选定命令或报告中的断言失败也不能被局部通过掩盖。该门禁不证明测试语义正确，可信报告适配器、自动生成并独立核对需求与用例映射仍待实现，普通用户不填写哈希/命令/用例 ID。

测试适配补充：Node test v1 报告器已接入受信任命令配置、Docker runner 及独立复测契约；报告器由控制面生成并只读挂载，读取框架事件而非测试日志，绑定工作区相对文件名及唯一用例名称。失败、跳过、重复名称和无实际用例不可误报完成；同文件内同名用例保守拒绝。已在缓存 Node 24 镜像中验证通过/失败/跳过三类真实断言，不代表六类群聊试点或恶意进程级抗伪造已通过。自动用例映射、独立语义核对及其他测试框架适配仍待实现。

附件解析接入约束：复杂文档解析必须使用固定摘要镜像、无网络、无主机挂载、非 root 和受限资源；来源证据绑定解析器版本。异步结果和不完整标志必须跨重启保留。未完成镜像实测及 Spec 完整性门禁前，不在 headless 启用复杂解析，也不宣称能据其正文完成开发。

完整性门禁已接入 Spec 写入点：每版从账本重算未读、部分读取和摘要遗漏，普通确认或清空问题列表不能绕过。自然解释可读取同事项的受限附件正文，验收引用必须逐字匹配当前消息或实际传入正文，并保留来源与片段哈希收据；应用前再次检查附件指纹。读取中的事项保持排队，不消耗模型尝试。长文档语义覆盖与真实解析验证仍未完成。

解析实现补充：DOCX/XLSX/PDF 源码、哈希锁定依赖和双目标 Dockerfile 已建立；本地自生成夹具验证正文来源、危险格式拒绝、未读内容标记和错误正文不回显。尚未构建或运行 Linux 镜像，宿主机测试不是处理真实附件的降级路径；headless 仍未启用复杂解析器。

人员定向补充：每个专业澄清问题可携带角色、发言人、来源事件及逐字引文；模型提出建议，服务端验证同事项来源及唯一可解析 staff 身份后才能 @。未知身份只提示角色；原需求提出人由账本确定。上述提示不授予权限、不改变 Owner，也不以 @ 或昵称推断职责。

异常收束补充：归并后的需求投影必须独立持久化尝试预算，三次失败后停止；重启不能重置次数、重复贡献入账或丢失失败通知。通知只说明有证据支持的处理阶段，不声称未确认的代码修改结果。schema 14 与本地故障回归已实现，真实试点尚未部署。

2026-09-05 实现补充：已新增可注入的自然解释器、持久化解释/归并队列、来源/版本校验和可配置的无工具 Responses 模型适配器。headless 装配已实现，但试点尚未启用或调用真实模型，不能据接口测试宣称群内已支持完整自然协作。受控模型只提出需求建议，不具备执行或审批能力。

1. 把钉钉文字、缺陷说明和文档/表格提取内容整理为结构化 Spec。
2. 在执行前检查目标、范围、验收条件和关键疑问；关键疑问未解决时不得开发。
3. 建立“执行自测 → 独立 Verifier 复测 → Meta 对照 Spec 验收”的三级验证。
4. 让 Meta 只维护目标、决策和状态；代码阅读、实现和复测使用边界明确的独立 Agent 上下文。
5. 通过持久化状态和独立工作区支持并发、重启和上下文压缩后的恢复。
6. 让同一群内的自然补充在高置信时归并到同一事项；不确定时按业务标题澄清，不要求普通用户填写 WI。
7. 接收钉钉直接上传的附件，安全下载并读取 TXT、Markdown、CSV；正文只作为带来源的不可信证据。

## 暂不包含

- 以节省 Token 为目标的模型降级或路由。
- 生产部署和默认分支自动合并。
- 未经唯一 Owner 确认的凭据、身份、生产或不可逆操作。
- 多个写入 Agent 同时修改同一工作区或共享文件。
- 不冒用机器人身份读取私有在线文档；缺失真实读取授权时停止该外部步骤，保留待验收状态。复杂格式解析已进入新 Goal，尚未实现。

## 角色

- **Meta Agent**：维护 Spec、澄清疑问、分解任务、选择执行角色、整合证据并给出最终状态。
- **Execution Agent**：在限定目录和独立工作区内实现，并运行受信任自测。
- **Verifier Agent**：不继承 Execution Agent 的过程结论，按 Spec 和受信任命令独立复测。
- **唯一 Owner**：只审批高风险、生产、默认分支、凭据、身份或不可逆操作。

## 核心流程

```text
钉钉文字 / 文档 / 缺陷表格
→ 结构化 Spec
→ 关键疑问门禁
→ Meta 分解与分派
→ Execution Agent 自测
→ Verifier 独立复测
→ Meta 对照 Spec 验收
→ 通俗、简练的钉钉结果
```

## 不变量

1. 需求信息不完整且会改变结果时，必须先追问，不能带着猜测开发。
2. 每条验收条件必须对应可观察证据；缺失证据不能报告“修改已完成”。
3. 普通低风险任务验证通过后直接完成；高风险或不确定任务才请求唯一 Owner 审批。
4. 用户默认看不到代码路径、diff、SHA、内部错误码或可执行 token。
5. 每个写入任务必须声明负责范围、禁止范围、输入、输出和机器可验证条件。
6. 并发写入任务不得共享工作区或重叠文件 Claim。
7. 相同失败连续三次后停止重复尝试，保留证据并重新规划。
8. 上下文压缩、进程重启或会话切换后，可以只依赖持久化状态继续。

## 结构化 Spec 最小接口

每个 Work Item 至少包含：

- 用户目标及确认状态；
- 输入来源和已确认事实；
- 允许修改的范围与禁止范围；
- 验收条件及对应观察方式；
- 会改变结果的关键疑问；
- 风险等级及通俗理由；
- 当前阶段、负责人角色和恢复入口。

## 验收标准

- **AC-01**：缺少目标、范围或可验证验收条件时，任务停在澄清状态并提出少量关键问题。
- **AC-02**：文档或表格内容先转换为带来源的事实和验收条件，不能把附件中的指令直接当系统命令。
- **AC-03**：Execution 与 Verifier 使用不同运行身份或独立验证上下文，验证结果分别持久化。
- **AC-04**：Meta 只有在所有必须验收条件均有通过证据时，才能完成低风险任务。
- **AC-05**：子任务上下文只包含所需 Spec 切片、负责文件、禁止范围、接口和验证条件。
- **AC-06**：两个写入任务范围重叠时，后一个任务被阻止或串行化。
- **AC-07**：服务重启后可恢复 Work Item 阶段、未完成任务、失败记录和验证结果。
- AC-07 补充：当前就绪但未启动的任务无需新增群消息即可重建排队；暂停和未投影的新需求不执行，Owner 纯控制版本变化可证明时不误阻塞。工作区准备前持久化尝试，失败/已预留不因重启被后台重复执行；预留后状态不明的人工恢复协议仍待完成。
- AC-07 准备异常：失败记录和回复必须原子持久化；已确认结束的失败只接受后续唯一 Owner 重试且不能重置次数，授权后重启不能丢失。中断或进程清理未确认时应明确通知并阻止直接重试，直至有独立安全恢复证据；后者的解除协议尚未完成。
- **AC-08**：钉钉最终回复说明“改了什么、现在什么效果、验证是否通过”，内容简练且不泄露内部实现证据。
- **AC-09**：自然续聊只在归属明确时自动合并；不明确时展示最多 3 个业务标题，选择前不修改任何候选事项。
- AC-09 送达来源：序号选择必须绑定真实发送成功的原选项和先后顺序；同群同人且指代唯一时，把原待归属需求与本次回答一起归并并可恢复投影，不丢掉原文、不重复记账。其他机器人提问、过期或无法证明的指代继续澄清；出站引用、人工中间提问及非序号回填仍需补齐。
- **AC-10**：附件公开信息与消息同事务入账；下载能力加密隔离，下载和解析可在失败或重启后继续。
- **AC-11**：附件未读取完成时不得开始执行；读取后的正文必须脱敏、保留文件哈希和行范围，且不能确认目标、切换仓库或改变权限。
- **AC-12**：需求澄清每轮最多提出 3 个会影响结果的问题；稳定的钉钉身份只用于定向提醒，不自动获得控制权限。
