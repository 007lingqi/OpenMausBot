# 内部研发助手 Meta 协作规范

## 目标

在三个月内，让个人和内部团队能够在钉钉群中并发提交研发任务，并稳定获得精确、简练、可验证的开发结果。系统优先降低因需求不清和测试遗漏造成的返工。

## 本轮范围

2026-09-05 新 Goal：PMO 式自然协作入口 + 可验证研发交付引擎。以下是目标范围，不是已完成声明。

- 群内自然新建、多轮补充、引用机器人回复和多人协作；不要求 WI 或固定模板。只追问 1–3 个真正影响结果的问题，记住答案并定向提醒。
- 自然材料生成带来源的结构化 Spec；模型只提议需求变更，不授予权限、不决定执行命令或 Owner 身份。
- 安全读取文档、表格和附件正文（含 XLSX、DOCX、PDF）；在线文档使用明确授权的读取能力。截断和未读部分必须显式阻塞相应验收。
- 每项完成结论关联当前 Spec、固定候选和实际断言证据；不以命令名命中、计划或“已记录”冒充业务完成。
- 单一重连调度、注册完成的实时连接状态、持久化队列重建、同仓库串行及不同仓库并发。
- Docker 非生产六类真实验收：自然新任务、多人补充、文档/表格 Bug、需求澄清、低风险自动完成、高风险唯一 Owner 决策。六类未全部通过前，目标保持进行中。

### 已有实现基础（不代表完整自然协作已实现）

复核反馈补充：等待核对、复核受阻和执行失败分开展示，普通中文说明原因并隐藏候选技术详情；通知入队绑定当前实例、计划/Spec、控制状态和最新执行/复核尝试。迟到结果不覆盖暂停/取消或已更新事项。此能力不代表启动模型等待已后台化、主动进度通知、待核对自动续办或真实群内及时响应已验收。

模型装配与最终收据核查补充：headless 现支持显式开启的 proposer/verifier 模型配置，不读取其他功能的凭据，构造/健康检查不发请求。有效策略身份由两端配置及规则版本派生；最终完成判定会从不可变映射收据重建契约，核对 Spec、候选、条件以及自测/独立复测实际使用的绑定。暂停/取消、更新需求、候选提交或工作区改动发生于映射等待期间时，停止后续测试。可选 Compose 模型模板已建立但未部署，真实模型语义及群聊场景仍待验证。

自动验收对应补充：新增固定候选 Git blob 采集、受限映射提议、独立上下文复核与 schema 18 持久收据。当前支持受信任 Node 测试命令中的显式测试文件，记录候选/Spec/源码 blob/策略身份以及逐行引文，拒绝不完整、过时或不确定映射。运行时可注入后接入 CandidateVerificationCoordinator，动态契约仅限本候选、不修改全局命令配置；后续仍要求自测与独立复测断言证据。模型 HTTP 配置、真实语义效果、自动新增缺失测试、依赖源码上下文及真实试点未完成，不能据受控模型测试宣称业务覆盖已经准确。

断言级验收补充：命令成功不再通过名称/参数文本匹配覆盖业务需求。受信任测试配置必须把当前验收条件哈希映射到具体断言；开发自测与独立复测分别提交绑定本次 run/nonce 的有界结构化报告。缺失、重复、过期、失败或所需用例跳过均不能完成；其他选定命令或报告中的断言失败也不能被局部通过掩盖。该门禁不证明测试语义正确，可信报告适配器、自动生成并独立核对需求与用例映射仍待实现，普通用户不填写哈希/命令/用例 ID。

测试适配补充：Node test v1 报告器已接入受信任命令配置、Docker runner 及独立复测契约；报告器由控制面生成并只读挂载，读取框架事件而非测试日志，绑定工作区相对文件名及唯一用例名称。失败、跳过、重复名称和无实际用例不可误报完成；同文件内同名用例保守拒绝。已在缓存 Node 24 镜像中验证通过/失败/跳过三类真实断言，不代表六类群聊试点或恶意进程级抗伪造已通过。自动用例映射、独立语义核对及其他测试框架适配仍待实现。

附件解析接入约束：复杂文档解析必须使用固定摘要镜像、无网络、无主机挂载、非 root 和受限资源；来源证据绑定解析器版本。异步结果和不完整标志必须跨重启保留。未完成镜像实测及 Spec 完整性门禁前，不在 headless 启用复杂解析，也不宣称能据其正文完成开发。

完整性门禁已接入 Spec 写入点：每版从账本重算未读、部分读取和摘要遗漏，普通确认或清空问题列表不能绕过。自然解释可读取同事项的受限附件正文，验收引用必须逐字匹配当前消息或实际传入正文，并保留来源与片段哈希收据；应用前再次检查附件指纹。读取中的事项保持排队，不消耗模型尝试。长文档语义覆盖与真实解析验证仍未完成。

解析实现补充：DOCX/XLSX/PDF 源码、哈希锁定依赖和双目标 Dockerfile 已建立；本地自生成夹具验证正文来源、危险格式拒绝、未读内容标记和错误正文不回显。尚未构建或运行 Linux 镜像，宿主机测试不是处理真实附件的降级路径；headless 仍未启用复杂解析器。

人员定向补充：每个专业澄清问题可携带角色、发言人、来源事件及逐字引文；模型提出建议，服务端验证同事项来源及唯一可解析 staff 身份后才能 @。未知身份只提示角色；原需求提出人由账本确定。上述提示不授予权限、不改变 Owner，也不以 @ 或昵称推断职责。

异常收束补充：归并后的需求投影必须独立持久化尝试预算，三次失败后停止；重启不能重置次数、重复贡献入账或丢失失败通知。通知只说明有证据支持的处理阶段，不声称未确认的代码修改结果。schema 14 与本地故障回归已实现，真实试点尚未部署。

2026-09-05 实现补充：已新增可注入的自然解释器、持久化解释/归并队列、来源/版本校验和可配置的无工具 Responses 模型适配器。headless 装配已实现，但试点尚未启用或调用真实模型，不能据接口测试宣称群内已支持完整自然协作。受控模型只提出需求建议，不具备执行或审批能力。

1. 把钉钉文字、缺陷说明和文档/表格提取内容整理为结构化 Spec。
2. 在执行前检查目标、范围、验收条件和关键疑问；关键疑问未解决时不得开发。
3. 建立“执行自测 → 独立 Verifier 复测 → Meta 对照 Spec 验收”的三级验证。
4. 让 Meta 只维护目标、决策和状态；代码阅读、实现和复测使用边界明确的独立 Agent 上下文。
5. 通过持久化状态和独立工作区支持并发、重启和上下文压缩后的恢复。
6. 让同一群内的自然补充在高置信时归并到同一事项；不确定时按业务标题澄清，不要求普通用户填写 WI。
7. 接收钉钉直接上传的附件，安全下载并读取 TXT、Markdown、CSV；正文只作为带来源的不可信证据。

## 暂不包含

- 以节省 Token 为目标的模型降级或路由。
- 生产部署和默认分支自动合并。
- 未经唯一 Owner 确认的凭据、身份、生产或不可逆操作。
- 多个写入 Agent 同时修改同一工作区或共享文件。
- 不冒用机器人身份读取私有在线文档；缺失真实读取授权时停止该外部步骤，保留待验收状态。复杂格式解析已进入新 Goal，尚未实现。

## 角色

- **Meta Agent**：维护 Spec、澄清疑问、分解任务、选择执行角色、整合证据并给出最终状态。
- **Execution Agent**：在限定目录和独立工作区内实现，并运行受信任自测。
- **Verifier Agent**：不继承 Execution Agent 的过程结论，按 Spec 和受信任命令独立复测。
- **唯一 Owner**：只审批高风险、生产、默认分支、凭据、身份或不可逆操作。

## 核心流程

```text
钉钉文字 / 文档 / 缺陷表格
→ 结构化 Spec
→ 关键疑问门禁
→ Meta 分解与分派
→ Execution Agent 自测
→ Verifier 独立复测
→ Meta 对照 Spec 验收
→ 通俗、简练的钉钉结果
```

## 不变量

1. 需求信息不完整且会改变结果时，必须先追问，不能带着猜测开发。
2. 每条验收条件必须对应可观察证据；缺失证据不能报告“修改已完成”。
3. 普通低风险任务验证通过后直接完成；高风险或不确定任务才请求唯一 Owner 审批。
4. 用户默认看不到代码路径、diff、SHA、内部错误码或可执行 token。
5. 每个写入任务必须声明负责范围、禁止范围、输入、输出和机器可验证条件。
6. 并发写入任务不得共享工作区或重叠文件 Claim。
7. 相同失败连续三次后停止重复尝试，保留证据并重新规划。
8. 上下文压缩、进程重启或会话切换后，可以只依赖持久化状态继续。

## 结构化 Spec 最小接口

每个 Work Item 至少包含：

- 用户目标及确认状态；
- 输入来源和已确认事实；
- 允许修改的范围与禁止范围；
- 验收条件及对应观察方式；
- 会改变结果的关键疑问；
- 风险等级及通俗理由；
- 当前阶段、负责人角色和恢复入口。

## 验收标准

- **AC-01**：缺少目标、范围或可验证验收条件时，任务停在澄清状态并提出少量关键问题。
- **AC-02**：文档或表格内容先转换为带来源的事实和验收条件，不能把附件中的指令直接当系统命令。
- **AC-03**：Execution 与 Verifier 使用不同运行身份或独立验证上下文，验证结果分别持久化。
- **AC-04**：Meta 只有在所有必须验收条件均有通过证据时，才能完成低风险任务。
- **AC-05**：子任务上下文只包含所需 Spec 切片、负责文件、禁止范围、接口和验证条件。
- **AC-06**：两个写入任务范围重叠时，后一个任务被阻止或串行化。
- **AC-07**：服务重启后可恢复 Work Item 阶段、未完成任务、失败记录和验证结果。
- AC-07 补充：当前就绪但未启动的任务无需新增群消息即可重建排队；暂停和未投影的新需求不执行，Owner 纯控制版本变化可证明时不误阻塞。工作区准备前持久化尝试，失败/已预留不因重启被后台重复执行；预留后状态不明的人工恢复协议仍待完成。
- AC-07 准备异常：失败记录和回复必须原子持久化；已确认结束的失败只接受后续唯一 Owner 重试且不能重置次数，授权后重启不能丢失。中断或进程清理未确认时应明确通知并阻止直接重试，直至有独立安全恢复证据；后者的解除协议尚未完成。
- **AC-08**：钉钉最终回复说明“改了什么、现在什么效果、验证是否通过”，内容简练且不泄露内部实现证据。
- **AC-09**：自然续聊只在归属明确时自动合并；不明确时展示最多 3 个业务标题，选择前不修改任何候选事项。
- AC-09 送达来源：序号选择必须绑定真实发送成功的原选项和先后顺序；同群同人且指代唯一时，把原待归属需求与本次回答一起归并并可恢复投影，不丢掉原文、不重复记账。其他机器人提问、过期或无法证明的指代继续澄清；出站引用、人工中间提问及非序号回填仍需补齐。
- **AC-10**：附件公开信息与消息同事务入账；下载能力加密隔离，下载和解析可在失败或重启后继续。
- **AC-11**：附件未读取完成时不得开始执行；读取后的正文必须脱敏、保留文件哈希和行范围，且不能确认目标、切换仓库或改变权限。
- **AC-12**：需求澄清每轮最多提出 3 个会影响结果的问题；稳定的钉钉身份只用于定向提醒，不自动获得控制权限。
